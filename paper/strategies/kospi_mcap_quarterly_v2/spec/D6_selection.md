# D6 선정

- 상태: `DRAFT` (2026-09-17 16시대)
- 층: 판단 층
- 게이트: G1 초안 / G2 코드 있음 / G3 합성 시험 통과 / G4 실행기에서 D2 산출 파일을 다시 읽어 연결
- 코드: `src/targets.py` `select_top`

## 입력
D2 산출 `universe_<선정일>.csv`

## 판정
```
시가총액 > 0 인 종목을 시가총액 내림차순, 같으면 코드 오름차순으로 정렬 -> 위에서 N=50
```
근거: 정의(시총 상위 N, N 은 자금 기준 — 3,000만원 → 50, 사용자 09-16. 09-17 자본 6,000만원으로 개정, N=50 유지). 정렬 규칙은 C6 블록

## 출력
`selection_<선정일>.csv` — 모집단 행 + `rank` / 요약 `n_names, selected, invalid_mcap_excluded`

## 경계
- 비중·수량을 정하지 않아요

## 안전장치
| 상황 | 판정 |
|---|---|
| 유효 종목이 N 미만 | 멈춤 `SELECTION_SHORT` — N 을 줄이지 않아요 (C8 A) |

## 실측 (2026-09-17 자료)
- 50종목 선정, 시가총액 0 이하 제외 0
- 50위 시가총액 약 15.45조원
