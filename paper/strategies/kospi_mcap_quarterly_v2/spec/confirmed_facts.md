# 확인된 사실 — 칸별로 가져다 쓰는 것

- 만든 날: 2026-09-17
- 뜻: 기존 로직(v41.1·topn·C1~C12 작업)을 검증하면서 **실측으로 확인된 사실**. 칸을 만들 때 해당 줄을 먼저 봐요
- 규칙: 출처가 있는 것만 적어요. 인용 전에 `CONCLUSION_REGISTER.md` 상태가 철회·재측정필요가 아닌지 확인해요

## D1 입력 데이터

| 사실 | 칸에 주는 영향 | 출처 |
|---|---|---|
| KRX 수동 다운로드 3종(전종목 기본정보·지정 현황·시세)은 로그인 후 사용자가 받을 수 있다 | 자동 수집 경로 없이 입력 확보 | PLANS (441) 09-15 15:44 실제 다운로드, (488) |
| KRX CSV 는 CP949 | 읽을 때 인코딩 명시 | 09-15 raw 기록, 09-17 재확인 |
| 단축코드에 영문이 섞인 종목이 있다(`0120G0` 등) | 코드는 문자열 6자리 그대로. 숫자 변환·zfill 로 망가뜨리지 않음 | PLANS (441) — 804 중 3개를 막던 버그 |
| 시가총액 = 종가 × 상장주식수, 오차 0 | 파일 값 교차 확인에 쓸 수 있음 | CONCLUSION_REGISTER 1절(2026-09-09, 2,578종목) |
| 단, KRX 전종목 시세에서 **KONEX 3종목은 불일치**(09-17). KOSPI 는 0 | KOSPI 만 쓰는 이 로직엔 영향 없음. 교차 확인은 상위 60 에서만 멈춤 | 09-17 16시 실측 |
| 같은 날 받은 3종은 결합 실패 0 (07-15, 09-17 두 번) | 상위 종목 결합 실패 = 입력 결함 | 09-17 실측 |
| KRX 3종은 **한 화면 `menuId=MDC0201020101`** 에서 받는다 | 안내 주소 하나 | 09-17 사용자 다운로드 출처 기록 |
| KOSPI200 오늘 종가는 20:05 수집 전엔 없다 → 16시 실행은 `INDEX_NOT_READY` | 선정일 실행 20:10 이후 | 09-17 실행기 실측 |
| 거래대금이 int32 로 감겨 24거래일 손상된 적이 있다 | 큰 숫자는 int64 로 읽기. 음수·작은 양수 이상치 검사 | 메모 value_int32_overflow (2026-09-10) |
| 가격 패널은 휴장일을 종가 0 으로 채운다(20%) | 패널을 쓰면 0 제거. KRX CSV 직접 사용이면 해당 없음 | CONCLUSION_REGISTER 1절 |
| 선택에는 **전일(선정일) 값만** 쓴다. 한 줄 차이로 +135%p 가 생긴다 | 집행일 가격으로 선정하지 않음 | MEASUREMENT_PROTOCOL 6 |
| KOSPI200 은 `index_daily_history.csv` 의 `index_code=2001`. 매일 20:05 수집(`VIBE_Index_Daily_Fetch`) | **선정일의 국면 판정은 20:10 이후** | SYSTEM_DEFINITION 고쳐야 할 것 절(이름 오기 주의), 09-17 파일 실측(09-16 행 20:09 수집) |
| 가드는 정규화 **전** 원본에 건다 (`"".zfill(6)=="000000"`) | 빈 코드 검사를 변환 전에 | 메모 guard_on_raw_not_normalized |
| 날짜 붙은 산출물은 30일 뒤 정리될 수 있다 | 결정 근거는 날짜 없는 append-only 기록에도 | 메모 dated_artifacts_get_deleted |

## D2 모집단

| 사실 | 칸에 주는 영향 | 출처 |
|---|---|---|
| KOSPI + 증권구분 `주권` + 주식종류 `보통주` = 804 (09-15 기본정보) | 필터 세 개가 필요. 주식종류만 보면 리츠·외국주권·DR 이 섞여 832 | 09-17 실측 |
| 시장구분에 `KOSDAQ GLOBAL` 이 따로 있다 | `KOSPI` 와 정확히 일치로 비교 | 09-17 실측 |
| 지정 현황 파일은 O/X 로 매매거래정지·정리매매·관리종목 등 11개 지정을 한 표에 준다 | 제도 차단 3종을 한 파일로 판정 | 09-17 실측(07-15 파일) |
| 2020년 이전 자료는 상장폐지가 0건(생존편향) | 과거 재현에 쓰지 않음 | SYSTEM_DEFINITION 생존편향 절 |
| 코스닥 제외가 맞다 (-4.73%) | 정의의 KOSPI 한정 근거 | SYSTEM_DEFINITION 실측 절(참고 등급) |

## D6 선정 · D7 목표 포트폴리오

| 사실 | 칸에 주는 영향 | 출처 |
|---|---|---|
| 동일가중보다 시총가중이 크게 낫다(+0.195) | 정의대로 시총가중 | SYSTEM_DEFINITION 09-13 밤 최종(참고 등급, 표본 안) |
| 비중 상한 20% 는 봉우리 정점, 무작위 대조를 크게 넘음 | 정의대로 20% | 같은 절 |
| 09-15·08-21 파일 기준 상위 50 에서 1위 비중이 35% → **상한 20% 가 실제로 걸린다** | 초과분 재배분 산식이 필요 | 09-17 실측(형식 시험용, 날짜 불일치) |
| 정수 주식 제약: 3,000만원 50종목이면 종목당 약 60만원, 못 담는 비중 약 7.8% | 정수화·잔여현금 규칙 필요 | SYSTEM_DEFINITION 09-14 개정 |
| 수량을 줄이는 장치가 여러 개면 **곱**을 봐야 한다(0.0095, 0.00375) | 축소 장치를 두지 않거나, 두면 곱을 산출물에 남김 | 메모 qty_reducer_product, PLANS (478)(479) |
| 3,000만원·50종목은 노출 100% 에서 정수화 통과(0주 7.2%, 현금 5.4%), **노출 50% 에서 실패**(0주 11.0%, 현금 26.7%). **6,000만원은 100% 통과(현금 4.4%), 50% 통과(투자 28,232,400 = 3,000만·100% 와 동일)** | 사용자 09-17 승인: 자본 6,000만원, N=50 유지 | D7 명세 실측(09-17 자료) |
| 부동소수 나눗셈 뒤 내림은 한 주를 잃을 수 있다(100000/10 → 9999) | 내림 전 작은 여유값 | D7 시험(09-17) |

## E1~E3 주문·발주·체결

| 사실 | 칸에 주는 영향 | 출처 |
|---|---|---|
| 한투 **시장가 매수는 상한가 기준 증거금** → 살 수 있는 수량이 줄어든다 | 주문 방식 **시장성 지정가**(사용자 09-17 결정) | 메모 market_order_upper_limit_margin (07-29) |
| 호가단위는 `utils/krx_tick.py`(유가증권 7단계)가 단일 출처. 5단계 표가 지정가 1,336,700 을 거래소 거절시킨 적 있음 | 지정가는 이 함수로 올림·내림 | `utils/krx_tick.py` 머리말(2026-09-08) |
| `kis_order_client.inquire_hoga` 는 숫자 6자리 코드만 받는다(영문 섞인 코드 거부) | 영문 코드 종목은 호가 불가 → 주문 불가로 빠짐 | `tools/kis_order_client.py:1425-1427`, 09-17 확인 |
| `kis_order_client` 주문 POST: 타임아웃은 재시도 없이 `UNKNOWN_PENDING`, **연결 오류는 `NETWORK retryable=True`** 로 올린다. 응답 중 끊겨도 접수됐을 수 있다 | 발송 중 모든 예외를 모르는 상태로, 조회 확인 전 재전송 금지 | `tools/kis_order_client.py:568-632`, 09-17 확인 |
| `place_order_cash` 도 숫자 6자리 코드만 받는다(발송 전 `ValueError`) | 발송 전 오류는 확실히 안 나간 것으로 구분 | `tools/kis_order_client.py:843-845` |
| 체결은 **주문번호로만** 귀속. 종목·방향으로 맞추면 남의 체결이 섞인다 | 원장 결합 키 = 주문번호 | OBJECTIVE_LEDGER v41.1 규정 절 |
| 모의계좌는 ID 당 1개. v41.1 루프가 같은 계좌를 쓴다(브로커 매수는 SKIP 규칙에 막힘, 09-16) | 전략 자본을 설정값으로 고정. 09-28 에 재확인 | PREREG topn 09-11 절, PLANS (476) |
| 모의 시장가 매도는 잔량을 순차로 채운다(377450 976주 중 3분 뒤 764주) | 부분체결·잔량 처리가 실제로 필요 | PLANS (492) 09-17 |
| 09-17 모의 서버: 잔고 4.5~6초, 주문 건당 ~4초, 체결 조회 3초, 실패 0 | 50건 발주 ≈ 3~4분 | PLANS (492) |
| 09-16 모의 서버: 잔고 조회 타임아웃·연결 끊김 각 1회 | 재조회·실패 기록 필요. 타임아웃을 "없음" 으로 읽지 않음 | PLANS (474) |
| 체결 기록으로 주문 파일을 만드는 **순서 역전**, 페이퍼 경로와 브로커 경로의 **판정 불일치** | 가져오지 않음. 주문 → 발주 → 체결 조회 순서만 | 뼈대 시험 `SKELETON_TRIAL_EXECUTION_LAYER_20260916.md` |
| 한투 응답 필드(09-17 실물 조회): 체결 조회 `odno·ord_gno_brno·pdno·sll_buy_dvsn_cd(01 매도/02 매수)·ord_qty·ord_unpr·tot_ccld_qty·tot_ccld_amt·avg_prvs·rmn_qty·cncl_yn·cncl_cfrm_qty·rjct_qty·ord_dt·ord_tmd`, 호가 `askp1·bidp1·askp_rsqn1·bidp_rsqn1·aspr_acpt_hour` | E3·E1 이 이 이름을 그대로 읽는다 | 09-17 17시 읽기 전용 조회 |
| 체결 금액은 `tot_ccld_amt` 를 쓴다. 평균가×수량과 다르다(377450: 15,532,064 vs 15,532,930) | 원장 금액 원천 | 09-17 topn 청산 조회 |
| 매수가능 조회: `nrcvb_buy_amt`(미수 없는 매수금액 104,418,011) vs `max_buy_amt`(**522,090,055, 증거금 포함**) | 매수 한도는 `nrcvb_buy_amt` 만. `max_buy_amt` 를 쓰면 미수가 난다 | 09-17 17:05 조회 |
| `ruse_psbl_amt` 101,075,710 = 당일 매도대금 재사용 가능액(topn 매도 101,379,910 에서 비용 뺀 값과 같은 크기) | **모의에서는 재구성일 매도 대금으로 같은 날 매수가 가능하다(조회상).** 실제 발주로는 09-22 시험에서 확인 | 09-17 17:05 조회 |
| 잔고 요약: `dnca_tot_amt` 3,342,301 / `prvs_rcdl_excc_amt`(D+2) 104,505,086 / `nass_amt` 104,505,086 | 계좌 점검은 D+2 예수금을 본다 | 09-17 17:05 조회 |
| 잔고 행 `prpr`(현재가) — **보유가 0 이라 아직 실물 미확인** | 실행기 평가가격. 09-22 시험 `balance_prpr_present` 로 확인 | 미확인 |
| 취소 뒤 원 주문 행의 `cncl_yn`·`cncl_cfrm_qty`·`rmn_qty` 가 어떻게 바뀌는지 — **미확인** | `CANCEL_NOT_CONFIRMED` 판정이 이 가정에 기댄다. 09-22 에 미체결 취소가 나오면 확인 | 미확인 |
| 모의 매수 수수료율 — **미확인**(설정 0.015% 는 Claude 제안) | 09-22 시험 `buy_fee_pct_inferred` 로 확인 | 미확인 |
| 주문 카나리아가 매일 09:20 같은 계좌에 1주 주문·취소를 낸다 | 집행일 발주는 09:20 카나리아와 겹치지 않게 | PLANS (492) |
| `kis_emergency_liquidate.py --allow-offhours` 는 동작하지 않는다 | 장외 발주 차단을 도구에 기대지 않음 | PLANS (485) |

## E4 원장 · 위험

| 사실 | 칸에 주는 영향 | 출처 |
|---|---|---|
| 실측 왕복 비용 0.343~0.358%, 매도세 0.2% | 비용 반영 | CONCLUSION_REGISTER 4절(2026-09-10) |
| **매도 대금은 결제일(D+2) 전까지 예수금 총액(`dnca_tot_amt`)에 안 들어온다.** 09-17 topn 매도 약 1억 137만원 뒤 09:51 계좌자산이 "보유 0 + 예수금 334만원" 으로 계산돼 낙폭 −96.9%(그림자 DDM Stage4) 오탐 | NAV·매수 가능 금액은 결제 전 매도 대금(D+1·D+2 예수금)을 따로 반영. 재구성일 매도 대금으로 같은 날 매수할 수 있는지 KIS 모의에서 확인 필요 | PLANS (495), `tools/broker_ledger_reconcile.py:141-143` |
| `x or default` 가 수수료 0.0 을 삼켜 손익을 3.5배 과대 계상했다 | 숫자 기본값에 `or` 금지 | 메모 pricing_engine_or_trap |
| "미검증" 을 PASS/FAIL 로 누르면 안 된다 — 세 번째 값 | 판정값에 미확인 칸 | logic_check_backlog (24)(25)(27) |

## 운영

| 사실 | 칸에 주는 영향 | 출처 |
|---|---|---|
| 휴장일 정본은 `holidays.json` | 선정일·집행일 계산 | 메모 measure_the_clock |
| 타임아웃(rc=124)은 원인을 지운다 | 타임아웃 시 원인 기록 필요 | 메모 timeout_erases_the_cause |
| 경보는 3.5개월 끊겼던 적이 있다(08-24 복구) | 알림 도착을 확인 대상으로 | 메모 alert_delivery_outage |
