# [2026-09-21] 루트에서 인자 없이 `pytest` 를 치면 루트·tools 의 `test_*.py` 를 수집한다.
#   그것들은 시험이 아니라 **이름만 test_ 인 일회용 스크립트**다 —
#   수집되는 순간 모듈 수준 코드가 실행된다(텔레그램 실제 발송, 장중 루프 import 등).
#   그래서 수집 범위를 tests/ 로 못 박는다.
[pytest]
testpaths = tests
