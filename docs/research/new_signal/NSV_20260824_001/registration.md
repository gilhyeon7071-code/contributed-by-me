# 사전등록 — NSV_20260824_001

**이 문서는 검정 실행 전에 고정되었다.** 이후 어떤 항목도 결과를 보고 바꾸지 않는다.

| 항목 | 내용 |
|---|---|
| `round_id` | `NSV_20260824_001` |
| `protocol_version` | `NEW_SIGNAL_VALIDATION_STANDARD.md` v1.0 (2026-08-24) |
| 검정 질문 | **ATR 중간 분위(D3~D6) 후보 바스켓은 동일 모집단 기준선보다 h5 초과수익이 양수인가** |
| 라운드 종류 | `EXPLORATION` — §3.3 에 따라 전 구간이 이미 열람되어 확증 불가 |
| 모집단 | KRX 전 종목. `close > 0`, ATR14 산출 가능(14거래일 이상), 거래대금 10억 이상(집행 가능성 제약이며 신호가 아니다). 주말 패딩(`close<=0`) 제거 |
| 데이터 스냅샷 | `E:\1_Data\krx_daily_archive\*_clean.parquet` (2015-01-02 ~ 2026-08-21). 날짜 정규화 `[^0-9]` 제거 후 `(date,code)` 중복 제거 |
| 가설 목록 | 단일 축 1개. `atr14_pct` 일별 십분위 **D3~D6** 동일가중 바스켓. **조합 1개만 검정한다** |
| 기준선 | 같은 신호일, 같은 적격 모집단 전체의 동일가중 수익률 |
| 주 지표 | `mean( 바스켓수익[D] − 기준선수익[D] )` over 신호일 |
| 주 보유기간 | **h5** (5거래일). 다른 보유기간은 보조 진단으로만 |
| 보조 지표 | 연도별 방향, 신호일 수, 후보 발생일 비율, 십분위별 분포 |
| 비용 | 왕복 0.358% (권위 원장 `trades_calc.csv` 기준). **바스켓과 기준선 양쪽에 동일 적용되어 초과수익에서 상쇄된다** |
| 시간 분할 | Train ~2019 / Validation 2020~2022 / OOS 2023~ |
| 최소 표본 | Train 고유 신호일 500일 이상 |
| `MDE` | **Train 0.0449%p** (n=1214, sd=0.5578%p, 검정력 80%, 양측 α=0.05) |
| `MES` | **0.358%p / h5.** 근거: 왕복 비용 1회분. 선정이 만드는 초과수익이 회전 비용 한 번분도 못 넘으면 회전하지 않고 유니버스를 드는 편이 낫다 |
| 검정 조합 수 | **1** (§4 기준 3 이하이므로 다중비교 보정 미적용) |
| 다중비교 통제 | 미적용. 사유: 사전등록 조합이 1개이며 변형을 시도하지 않는다 |
| 실패 기준 | Train 주 지표가 MES(0.358%p) 미만이면 `NOT_SUPPORTED`. 0 이하이면 명확한 실패 |
| 보류 기준 | Train 신호일 500일 미만이면 `DEFERRED_INSUFFICIENT_SAMPLE` |
| 운영 분리 선언 | 운영 Gate, LOCK, 주문, 브로커, paper runtime 미변경 |

## §4.1 시작 금지 규칙 검사

```
MDE 0.0449%p  <  MES 0.358%p        →  통과. 시작한다
검정력 여유 약 8배
```
**효과가 MES 만큼 없으면 그것은 표본 부족이 아니라 진짜 음성이다.** 이 라운드는 그것을 구분할 수 있다.

## §3.3 에 따른 상한 선언

이 가설은 2026-08-24 에 전 구간 십분위 관측을 본 뒤 세운 **사후 가설**이다.
따라서 이 라운드는 `EXPLORATION` 이며, 결과가 아무리 좋아도
**`EXPLORATION_CANDIDATE` 를 넘어설 수 없다.**

확증은 전진 관측으로만 가능하다. 이 라운드의 실질적 목적은 두 가지다.

1. Train 에서 효과 크기를 재어 **전진 관측으로 확증하는 데 필요한 기간을 계산**하는 것
2. 효과가 MES 에 못 미치면 이 방향을 **여기서 접는 것**

## 실행 전 확인

- [x] 기준서 v1.0 §1~§16 읽음
- [x] MDE 계산 후 §4.1 통과 확인
- [x] 조합 수 1개로 고정, 변형 시도 안 함
- [x] 주 보유기간 h5 고정
- [x] 실패 기준을 실행 전에 기재
- [x] 운영 무변경
