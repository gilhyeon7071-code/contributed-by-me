# RD_20260831_flow_h10 — 종결

- 종결일: 2026-09-19 (사용자 승인)
- 상태: **CLOSED**. `frozen.json`·`registration.md` 는 바꾸지 않았다(증거 보존)

## 왜 종결하나

1. **기준선이 두 번 움직였다.** 동결 뒤 `tools/load_merged_panel.py` 가 2026-09-10 거래대금
   int32 수리로 바뀌었다(b816aa28 -> 2a36ccfd). 그 파일이 기준선에 들어 있어
   **동결 전후 측정을 비교할 수 없다.**
2. `2_Logs/index_daily_history.csv` 접두 해시 불일치는 **수집 시각 열 때문**이고 값은 그대로다.
   2026-09-19 재측정으로 다시 확인했다 — KOSPI200 08-10~08-28 14개 날짜 전부 저장값과 일치.
   (이미 2026-09-08 `exceptions.md` 에 같은 결론이 있다)
3. 이 라운드의 질문("수급 축이 h10 에서 초과수익을 만드는가")은 **2026-09-18 H5 측정으로 대체**됐다.
   그 측정은 대조군·날짜 단위 판정·검정력을 갖췄고, 결과는 1일 보유 스프레드 +0.11%(p=0.164),
   최소 탐지 0.40%p — **효과가 있더라도 왕복 비용 0.35% 보다 작다**.

## 종결이 뜻하는 것

- 이 라운드로는 더 이상 판정하지 않는다. 매일 나가던 `[FAIL] 동결 위반` 경보도 멈춘다
- 수급 축을 다시 보려면 **새 라운드로 사전등록**한다(원장: `docs/references/H1_TRIALS_LEDGER.md`)
- 종결 표시는 이 파일 하나다. `round_freeze_watch` 가 이 파일을 보고 CLOSED 로 읽는다
