# 사전등록 - RD_20260831_flow_h10

**수급(투자자별 순매수) 축이 h10 에서 초과수익을 만드는가**

- 등록 시각: 2026-08-31T09:29:20
- 상태: **FROZEN** (2026-08-31T09:32:07 동결. 이 문서는 수정하지 않는다)
- 규격: docs/references/NEW_SIGNAL_VALIDATION_STANDARD.md
- 이 문서는 **동결 후 수정하지 않는다.** 바꿔야 하면 새 round_id 를 연다.

## 등록표 (기준서 §4)

| 항목 | 값 |
|---|---|
| round_id | RD_20260831_flow_h10 |
| kind | CONFIRMATION |
| 가설 | 외국인+기관 순매수를 거래대금으로 정규화한 값의 상위 20% 바스켓이 하위 20% 바스켓보다 h10 수익이 높다. 거짓이면 스프레드 평균이 0 이하이거나 부호가 반대다 |
| 기준선 | 같은 신호일 같은 유니버스의 하위 20% 동일가중 바스켓(롱숏 스프레드). 같은 날 같은 유니버스이므로 스타일은 자동 일치. 보조 기준선으로 구성일치 ETF 혼합(069500/229200)을 둔다 |
| 기준선_파일 | tools/load_merged_panel.py, tools/fetch_investor_flow.py, 2_Logs/index_daily_history.csv |
| 주지표 | h10 롱숏 스프레드(상위20% 평균 - 하위20% 평균). 신호일별로 계산한 뒤 신호일 평균 |
| 표본단위 | 종목-일 (바스켓 구성 단위) |
| 클러스터단위 | 신호일. 유의성은 신호일 표준오차로 잡는다. 종목-일 단위 t 는 쓰지 않는다 |
| n | 280 (1년 거래일. 2026-08-31 기준 30일 확보, 잔여 250일) |
| MDE | 0.582 %p. 산출 2.8016 x sd(신호일별 스프레드) / sqrt(280). sd 는 2026-07-16~08-28 30거래일 실측(2_Logs/design/flow_axis_mde.csv 외국인 h10 행) |
| MES | 1.0 %p. 근거 왕복비용 0.358%p 의 2.8배이며 D 트랙이 정한 매수신호 합격선(h10 알파 1%p)과 같다 |
| 판정규칙 | 280 신호일 수집 후 세 조건을 모두 만족해야 SUPPORTED. (1) 스프레드 95%CI 하한 > 0 (2) 점추정 >= MES 1.0%p (3) 롱 다리 단독이 유니버스 동일가중을 비용 차감 후 상회. 하나라도 어긋나면 NOT_SUPPORTED. 280일 미달이면 DEFERRED_INSUFFICIENT_SAMPLE 이며 어떤 판정도 하지 않는다 |
| 중단조건 | 140 신호일 중간점검에서 점추정이 음수이고 95%CI 상한 < MES 이면 즉시 중단하고 NOT_SUPPORTED 로 닫는다. 수집 배치가 30일 연속 실패해도 중단한다 |
| 검정조합수 | 1 (신호 외국인+기관 합, 지평 h10 단일). 신호·지평·분위수 변형은 이 라운드에 포함하지 않는다 |
| 다중비교 | 조합수 1이므로 미적용. 단 이 가설을 낳은 탐색(신호 4종 x 지평 3종 = 12조합)은 2_Logs/research_trial_ledger.json 에 기록돼 있다 |
| 운영분리 | 운영 Gate/LOCK/주문/브로커/paper runtime 미변경. 패널 측정이며 실매매·가상매매와 무관하다 |

## 이 가설의 출처

**사후 가설이다.** 2026-08-31 PLANS (145) 의 탐색에서 나왔다.
30거래일 24,510 관측으로 신호 4종 x 지평 3종을 훑었고 그 중 외국인 h1 만 t=2.24 였다.
h10 은 어느 신호도 유의하지 않았다(절대값 t < 0.5).
**즉 이 라운드는 유망해서 여는 것이 아니라 검정력이 확보되는 유일한 축이라서 여는 것이다.**

선행 사실:
- 가격·거래대금 15축은 h10 알파가 전부 0.46%p 이하로 합격선(1%p) 미달이었다
- 수급은 가격과 독립인 축이고 후보 목록이 아니라 거래대금 기준 유니버스에서 나온다(축 순환 없음)
- h1 은 LS 스프레드 0.296%p 로 왕복비용 0.358% 보다 작다. 그래서 h1 이 아니라 h10 이다

## 한계 (함께 인용할 것)

(1) sd 추정이 30거래일 표본에서 나왔다. 실제 sd 가 커지면 MDE 가 커지고 여유(0.418%p)가 줄어든다.
    140일 중간점검에서 sd 를 재추정하고 MDE 가 MES 를 넘으면 그 시점에 라운드를 닫는다.
(2) KIS 는 최근 30거래일만 준다. 수집 배치가 멈추면 그 구간은 영구 결손이다.
(3) 유니버스가 매일 거래대금 기준으로 재산출되는 불균형 패널이다. 종목 진입·이탈은 통제하지 않는다.
(4) 롱숏 스프레드는 신호 존재 검정이다. 한국 공매도 제약 때문에 그 자체가 운용 가능한 형태는 아니다.
    운용 가능성은 판정규칙 (3) 의 롱 다리 조건으로만 본다.
