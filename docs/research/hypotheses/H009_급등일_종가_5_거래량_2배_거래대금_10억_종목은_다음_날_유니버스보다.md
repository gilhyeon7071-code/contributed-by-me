# H009 — 급등일(종가 +5%·거래량 2배·거래대금 10억) 종목은 다음 날 유니버스보다 더 오른다

- 등록 시각: **2026-09-19 15:05:56**  (이 시각 이후의 측정만 사전등록으로 인정돼요)
- 상태: `DONE`  (측정 시작 시 `MEASURING`, 끝나면 `DONE` / `DROPPED`)

> **작성 후 본문을 고치지 마세요.** 생각이 바뀌면 새 가설을 등록해요 — 그것도 시도 1건이에요.

---

## 1. 어디서 왔나 (필수)

<!-- 관찰 / 문헌 / 기존결과에서 파생 / 우연 중 무엇이고, **구체적으로 무엇을 보고** 떠올렸는지.
     "그냥 해봤다" 도 정직한 답이에요. 그렇게 적으면 나중에 그 사실을 알 수 있어요. -->

출처 종류: 기존 로직(v41.1 급등 매매)의 아이디어를 구현 결함과 분리해 재는 것 — 사용자 요청 2026-09-19 "급등 아이디어 진행"
근거: v41.1 급등 실현 매매는 게이트 통과분이 진입일 클러스터 지수 대비 −3.69%(t −3.36, PLANS 526)였으나 −3% 손절·결측 호가 통과 등 구현 결함 4건이 섞여 아이디어 값이 분리되지 않았다. 정의는 `tools/surge_detector_realtime.py` 실효 임계값(스냅샷 thresholds: pct_min 0.05, rvol20_min 2.0, trading_value_floor 1e9) 그대로

## 2. 기계적 주장 (필수)

<!-- 부호까지 포함해 한 문장으로. 이 칸이 `rs` 사건을 막는 자리예요.
     예: "외국인 순매수 **금액이 큰** 종목이 10거래일 뒤 유니버스보다 **더 오른다**"
     정의도 적어요 - 무엇을 어떻게 계산하는지. "비"인지 "차"인지가 부호를 바꿔요. -->

주장: 급등일 종목의 **다음 날 수익률이 같은 날 적격 유니버스 동일가중 수익률보다 높다**(추세 지속). 비용(왕복 0.358%)을 넘어야 매매 가치
축의 정의: 일봉 재구성 PRICE_VOL_BREAKOUT — t일 종가 등락률(close_t/close_{t-1}−1) ≥ 5% AND 거래량비 ≥ 2.0 (거래량 = value/close, 비 = t일 거래량 / t−20~t−1 평균) AND value_t ≥ 10억. 등락률 22% 이상(상한 근접)은 별도 유형으로 분리 보고. 적격 유니버스 = 같은 날 value ≥ 10억·전일 종가 있음·급등 아닌 종목. 가격 패널 `tools/load_merged_panel.py`, 일간 |등락률| > 30.5% 행 제거(기업행위), close>0
지평(h): **주 = h1**: t 종가 진입 → t+1 종가(감지 당일 종가가 장중 감지 뒤 실행 가능한 가장 늦은 가격). 보조 진단만: h3·h5(t 종가 기준). 결과 보고 바꾸지 않음. 구간: 탐색 2015–2021 / 검증 2022–2023 / OOS 2024–2026-08

## 3. 왜 그래야 하나 (기전)

주장 쪽: 거래량을 동반한 급등은 정보 도착 — 정보가 한 번에 반영되지 않으면 다음 날 이어진다. 반대 기전: 개인 과잉반응·유동성 압력 → 다음 날 되돌림. 어느 쪽이 큰지가 질문이다

<!-- 시장에서 무슨 일이 일어나서 그 결과가 나오는지. 기전이 없으면 우연과 구별되지 않아요. -->

## 4. 기존 축과의 관계

v41.1 일반 진입(v_accel·rs, 5일 −2.33%p, 참고용)과 겹칠 수 있음 — 상관 미확인. 이번은 급등 정의 단독(단일 축, 기준서 9절 1단계)

<!-- 이미 있는 축과 겹치나요? 겹치면 새 시도가 아니라 재측정이에요.
     상관을 재봤다면 값을, 안 재봤으면 "미확인" 이라고 적어요. -->

## 5. 틀렸다면 어떻게 아나 (반증 조건)

- 주 지표(h1 신호일 평균 초과) ≤ 0 → 추세 지속 기각(NOT_SUPPORTED). 음수이고 신호일 클러스터 t ≤ −2 → **되돌림**(차단 필터 후보로만 의미)
- 0 < 주 지표 < 0.358% → WEAK_SUPPORT(현상은 있으나 매매 가치 없음)
- 주 지표 ≥ 0.358% 이고 t ≥ 2 이고 탐색·검증·OOS 세 구간 모두 같은 부호 → SUPPORTED
- 한 해·한 구간에만 의존하면 NOT_SUPPORTED
- 보조 필수: 신호일 vs 비신호일 시장 수익 분포(선택편향), 연도별 부호

<!-- 어떤 결과가 나오면 이 가설을 버리는지. 미리 적지 않으면 나중에 해석이 늘어나요. -->

## 6. 잴 수 있나 (검정력)

<!-- **이 칸이 비면 측정하지 마세요.** 결함 4번(검정력 부재)이 여기서 막혀요.
     필요 표본 = (2.8 x sd / 목표알파)^2 블록.  겹침 보정: 블록 1개 = h 거래일.
     sd 를 모르면 "소표본으로 sd 부터 추정" 이라고 적고 그 결과를 여기에 채워요. -->

목표 알파: 0.358%/신호일 (h1 왕복 비용 — 이보다 작으면 매매 가치 없음)
관측 sd: 미측정 — 가정 3.5%(급등 바스켓 일간 초과 변동), 측정 후 실측값으로 재계산해 결과 칸에 적는다
필요 블록: (2.8×3.5/0.358)² ≈ 750 신호일 (h1 이라 겹침 없음, 블록 = 1일)
필요 거래일: 신호일 750 이상
현재 보유: 패널 2015-01-02~2026-08-24 약 2,800 거래일, 급등 신호일 수는 측정 첫 단계에서 센다
-> 잴 수 있나: 예(신호일 ≥ 750 이면). 미만이면 DEFERRED_INSUFFICIENT_SAMPLE

---

## 결과 (측정 후에만 채워요)

<!-- 측정 전에는 비워둬요. 여기가 채워졌는데 등록 시각보다 이른 측정이면 사전등록이 아니에요. -->

측정 시각: 2026-09-19 15:1x (등록 15:05:56 이후)
결과: 패널 2015-01-05~2026-09-18, 급등 132,784행 / 신호일 2,798(전 거래일의 98%, 하루 중간값 44종목). 실측 sd 1.645% -> 필요 신호일 (2.8×1.645/0.358)² ≈ 166, 검정력 충분.
- **주(h1, 5%≤등락<22%)**: 전체 −0.020%(t −0.64) / 탐색 2015–21 **+0.078%(t 1.99)** / 검증 2022–23 **−0.189%(t −2.89)** / OOS 2024–26 **−0.159%(t −2.14)**. 연도 양수 6/12, 2026 −0.81%
- 보조 h3 −0.034%(t −0.72), h5 −0.187%(t −3.16, 양수 3/12)
- 분리 보고 유형(등록한 별도 유형) 상한근접 ≥22%: h1 **+1.618%(t 12.7)**, 세 구간 모두 +1.5~1.7%, 12/12 양수. **종가 매수 가능성 미확인** — 상한가(+30%) 잠김 종목은 종가에 체결 안 될 수 있음(대기열), 체결되는 날은 잠김이 풀리는 날이라 역선택 가능
- 선택편향: 신호일이 98% 라 비신호일(52일) 비교는 정보 없음
- 한계: 장중 감지 가격이 아니라 종가 진입(시가·고가 없음, 패널에 open 없음). 탐색 구간 패널은 코스피 위주, 2022-10 이후 코스닥 포함 — 구간 간 부호 반전에 구성 변화가 섞였을 수 있음(미분리)
판정: **NOT_SUPPORTED (주 가설, 추세 지속)** — 구간 간 부호 반전, 비용 0.358% 에 한참 못 미침. 최근 4년은 약한 되돌림. 상한근접 지속은 주 가설 밖의 관측이라 채택 근거 아님 → 매수 가능성을 묻는 새 가설로 별도 등록해야 잴 수 있음
산출물: `H009_artifacts/measure_h009.py`, `h009_result.json`
