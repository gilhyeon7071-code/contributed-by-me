# 자동매매 정합성 검증 프레임워크 (2026-03-09)

## 목적
- 구현 완료 체크(기능 존재)와 별도로, 운영 가능한 수준의 정합성(무결성/상태/배치)을 검증한다.
- 기준은 9개 정합성 축:
  - 개체무결성
  - 참조무결성
  - 도메인무결성
  - 비즈니스규칙검증
  - 배치검증
  - 체크섬
  - 샘플링검증
  - 입력값검증
  - 상태전이검증

## 실행 방법
1. 기존 시스템 검증 실행
```bat
E:\1_Data\run_system_verification.bat
```

2. 정합성 오버레이 실행
```bat
E:\1_Data\run_integrity_overlay_report.bat
```

3. 산출물 확인
- `E:\1_Data\2_Logs\integrity_overlay_latest.json`
- `E:\1_Data\docs\03-operations\integrity_overlay_20260309.md`

## 1~9단계 정합성 게이트
| 단계 | 핵심 정합성 축 | 통과 기준(요약) |
|---|---|---|
| 1단계 기본 인프라 | 입력값검증, 도메인무결성, 상태전이검증 | 키/계좌 형식 검증, 토큰 발급/갱신 정상, mock/prod 전환 일관 |
| 2단계 시세 데이터 | 개체무결성, 도메인무결성, 상태전이검증, 배치검증 | 종목코드/호가/거래량 스키마 정상, websocket 재연결 및 누락 복구 |
| 3단계 매매 조건 | 비즈니스규칙검증, 상태전이검증, 샘플링검증 | 매수/매도/손절/익절 충돌 없음, 조건 충족 로그 일관 |
| 4단계 주문 실행 | 참조무결성, 상태전이검증, 비즈니스규칙검증 | 주문요청-응답-체결-미체결-취소 체인 정합, 중복/거부 처리 정상 |
| 5단계 리스크 관리 | 비즈니스규칙검증, 도메인무결성, 상태전이검증 | 한도 초과 차단, 킬스위치/긴급청산 즉시 동작 |
| 6단계 잔고/수익 | 참조무결성, 도메인무결성, 샘플링검증 | 잔고/체결/손익 대사 일치, 수수료/세금 계산 일관 |
| 7단계 로그/모니터링 | 체크섬, 배치검증, 상태전이검증 | 로그 무결성, 알림 전달, 스케줄 실행 누락 없음 |
| 8단계 가상매매 테스트 | 상태전이검증, 샘플링검증, 배치검증 | E2E/장시간/장애주입 시 fail-closed + 자동복구 확인 |
| 9단계 실전 전환 | 비즈니스규칙검증, 참조무결성, 배치검증 | 소액 canary + 모의/실전 비교 + 정기 점검 자동화 |

## 현재 스냅샷 (source: verification_report_20260309_105813.json)
- 전체 항목: 55
- 정합성 매핑: 55/55 (미매핑 0)
- 카테고리 커버리지: 9/9

카테고리별 pass_rate:
- 개체무결성: 100.0%
- 참조무결성: 80.0%
- 도메인무결성: 76.9%
- 비즈니스규칙검증: 73.9%
- 배치검증: 90.9%
- 체크섬: 100.0%
- 샘플링검증: 57.1%
- 입력값검증: 100.0%
- 상태전이검증: 96.0%

## 운영 규칙
- 배포/실전 전환 전 필수 조건:
  - `state_transition_validation` pass_rate >= 95%
  - `business_rule_validation` fail = 0
  - `referential_integrity` fail = 0
  - `batch_validation` fail = 0
- 개선 우선순위:
  1. 샘플링검증 (pass_rate 57.1%)
  2. 비즈니스규칙검증 (pass_rate 73.9%)
  3. 도메인무결성 (pass_rate 76.9%)

## 강화 게이트(자동 차단)
- 실행: `E:\1_Data\run_integrity_gate.bat`
- 기본 기준(STRICT)
  - 샘플링검증: pass_rate >= 65.0, fail_count <= 0, warn+skip <= 2
  - 비즈니스규칙검증: pass_rate >= 75.0, fail_count <= 0, warn+skip <= 6
  - 도메인무결성: pass_rate >= 80.0, fail_count <= 0, warn+skip <= 4
- 현재 기준 미달 시 exit code `2` (메인 배치에서는 `42`로 승격하여 차단)

메인 배치 연동:
- `run_system_verification_easy.bat`에서 기존 검증 후 자동 실행됨
- 제어 변수
  - `VERIFY_INTEGRITY_GATE=1` (기본)
  - `VERIFY_INTEGRITY_GATE_MODE=STRICT|WARN` (기본 STRICT)
