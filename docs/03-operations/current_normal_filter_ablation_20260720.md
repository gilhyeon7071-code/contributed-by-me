# Current Normal Filter Ablation — 2026-07-20

## 지시사항

현재 일반 매매 경로의 두 차단 조건이 후보 품질을 실제로 개선하는지 읽기 전용으로 확인한다.
운영 Gate, 주문, 체결, 원장, 설정값은 변경하지 않는다.

## 고정한 검증 범위

- 대상: 현재 후보 생성기의 고정 L0 AND 선택기만 통과한 역사 후보.
  섹터·뉴스·최종점수·보존된 후보 파일은 포함하지 않는다.
- 가격 계약: `PRICE_HISTORY_INTEGRITY_V1`; 실제 전역 거래일 기준.
- 진입/청산: 후보일 다음 실제 거래일 시가 진입, h5 실제 거래일 종가 청산.
- 비용: 매수·매도 수수료 각 0.5%, 매수·매도 슬리피지 각 0.1%, 매도세 0.2%.
- 주 지표: 신호일 동일가중 바스켓의 비용 차감 h5 수익률.
- 분할: Train 2020–2023, Validation 2024, OOS 2025–2026H1.
- 최소 신호일: 20일 미만이면 `DEFERRED_INSUFFICIENT_SAMPLE`.
- 파라미터 탐색·운영 정책 변경: 없음.

## 검증 1 — 갭 위험 이력 차단

고정한 현재 설정은 `lookback_sessions=20`, 일간 시가 갭 `<= -8%`, 최근 하락갭이 1건 초과일 때 차단이다.

| 구간 | 전체 후보 | 갭 차단 통과 | 갭 차단 대상 | 판정 |
|---|---:|---:|---:|---|
| Train 2020–2023 | 57행 / 51일 | 57행 / 51일 | 0행 / 0일 | 차단 효과 보류 |
| Validation 2024 | 23행 / 23일 | 23행 / 23일 | 0행 / 0일 | 차단 효과 보류 |
| OOS 2025–2026H1 | 40행 / 37일 | 40행 / 37일 | 0행 / 0일 | 차단 효과 보류 |

전체와 통과군의 수익률이 같은 이유는 세 구간 모두 차단 대상이 0건이기 때문이다. 따라서 이 실행은 갭 차단이 유효하다는 근거도, 불필요하다는 근거도 제공하지 않는다.

참고로 OOS 전체/통과군의 평균 일별 순 h5 수익률은 +194.6781bp이나, 중앙값은 -67.6673bp이다. 이는 표본의 분포가 비대칭임을 보여 주는 기술 통계일 뿐, 갭 차단의 효과나 전략 승격 근거가 아니다.

## 현재 런타임 차단 2건과의 관계

2026-07-20 P1 런타임에서는 보존된 최종 후보 6건 중 2건이 `GAP_RISK_HISTORY_BLOCK`이었다. 이번 역사 L0 집합은 120행 중 0건이 차단되었다.

두 집합은 동일하지 않다. 런타임 P1은 보존된 최종 후보 파일을 입력으로 사용하고, 이번 검증은 의도적으로 현재 생성기의 L0만 재현했다. 그러므로 이 차이는 갭 정책의 성과 차이가 아니라 후보 입력 경로가 다르다는 사실이며, 둘을 합쳐 효과 판정에 사용하면 안 된다.

## 검증 2 — 장중 모멘텀 재확인

현재 조건은 역사 `v_accel`에 장중 거래대금/역사 거래대금 비율을 곱해 재계산한 값이 1.0 미만이면 차단한다.

이 조건은 과거 장중 거래대금 스냅샷이 있어야 재현된다. 현재 보관된 장중 가격 파일은 2026-07-20 단일일뿐이므로, 2020–2026 역사 후보에 대한 통과/차단·수익률 비교는 수행하지 않았다. 이를 일별 거래대금으로 대체하면 다른 정책을 검증하는 것이 되므로 금지한다.

판정: `NA — 역사 장중 스냅샷 부재`.

## 산출물 및 직접 실행 증거

- 실행 스크립트: `E:\1_Data\tools\audit_current_normal_gap_filter_ablation.py`
- 결과 JSON: `E:\1_Data\2_Logs\current_normal_gap_filter_ablation_20260720.json`
- 결과 CSV: `E:\1_Data\2_Logs\current_normal_gap_filter_ablation_20260720.csv`
- 실행 로그: `E:\1_Data\2_Logs\current_normal_gap_filter_ablation_20260720.run2.log`
- 가격 범위: 2020-01-02 ~ 2026-07-16, 무결성 계약 적용 후 2,083,264행.
- 고정 L0 후보: 120행, 신호일 111일; h5 평가 가능 120행.
- 문법: `python -m py_compile` PASS.
- 실행: 결과 JSON의 `status=OK` 확인.

## 검증 상태

| 항목 | 상태 | 근거 |
|---|---|---|
| 기능 | PASS | 가격 적재, L0 재현, 결과 파일 생성 완료 |
| 정합성 | PASS | 현재 설정·가격 계약·실제 거래일·비용을 고정해 적용 |
| 운영 반영 | NA | 읽기 전용 검증이며 운영 경로를 변경하지 않음 |
| 정책 | PASS | 임계값·Gate·주문 정책 변경 없음 |
| FAIL-CLOSED | PASS | 주문·체결·원장·브로커 경로 미변경 |
| 회귀 | PASS | 진단 스크립트 문법 및 결과물 생성 확인; 운영 소스 미변경 |

## 결론 및 남은 실제 문제

- 갭 위험 차단: 현 L0 역사 표본에서 차단 사례가 0건이므로 효과 판정은 보류다.
- 장중 모멘텀 차단: 역사 장중 거래대금 스냅샷이 없어 효과 검증은 NA다.
- 따라서 이번 결과는 현 일반 매매 차단 정책을 유지·삭제·완화하는 근거가 아니다.
- 다음 검증을 하려면, 먼저 런타임 보존 후보와 연구 L0 후보가 왜 다른지 입력 경로를 분리해 기록해야 한다. 이는 전략 확장이 아니라 현재 로직의 후보 입력 계약 확인이다.