# 새 로직 첫 재구성 — 2026-10-01 집행 ExecPlan

- 작성: 2026-09-16 22:09
- 상태: `ACTIVE`
- 결정: 사용자 2026-09-16 22:0x — 첫 재구성일 A(09-30 선정 / 10-01 집행), 09-28 가부
- 1층: `docs/references/NEW_LOGIC_L1_PURPOSE_20260916.md` (09-13 시스템 정의 + 09-16 결정)
- 뼈대: `docs/references/TRADING_LOGIC_SKELETON_20260916.md` v3 — 검증 주장의 틀이지 수정 관문이 아님
- 이름: **분기 시총 / `KOSPI_MCAP_QUARTERLY_V2`** — 작업 폴더 `paper/strategies/kospi_mcap_quarterly_v2/` (V1 과 분리)
- 용어: `docs/references/NEW_LOGIC_GLOSSARY.md` (09-17 확정, 사용자 "너의 제안대로 진행"). 이 문서의 기존 표현(리밸런싱·기준일·가상매매 등)은 다음 수정 때 용어집 말로 바꿔요
- 방식: 새로 만든다. 칸마다 필요한 블록은 그때 기존 자산(C1~C12 계약, v41.1·topn 도구)에서 게이트로 확인하고 가져온다

---

## 1. 이번에 검증하는 것 — 성과가 아니라 흐름

```
09-30 장후   D1 입력 → D2 모집단 → D3 국면 → D6 선정(시총 상위 50) → D7 목표 포트폴리오(6,000만원 — 09-17 개정)
10-01 장중   E1 주문(목표 − 보유) → E2 상태기계 → E3 체결
10-01 장후   E4 원장·NAV → 사고 4종 판정
```

**합격 = 위 흐름이 정의대로 한 번 돈다 + 사고 4종(시스템 정의 R10) 0건.**
성과(수익률)는 보지 않아요. 3년 판정·실매매 전환 조건(R5)은 이 검증 뒤에 정해요.

## 2. 고정 조건

| 항목 | 값 | 출처 |
|---|---|---|
| 모집단 | KOSPI 보통주, 거래정지·관리·상폐절차 제외 | 정의 + C8 |
| 선정 | 시총 상위 N=50 | 정의 09-14 개정, 사용자 09-16 |
| 가중 | 시총가중, 개별 상한 20% | 정의 09-13 밤 최종 |
| 국면 | KOSPI200 종가 < MA200 → 보유 50% (판정 t 종가, 효력 t+1) | 정의 8, C5 |
| 자금 | ~~3,000만원~~ **6,000만원** 기준 | 사용자 09-16 → **09-17 개정**(노출 50% 정수화 통과용, 아래 개정 이력) |
| 선정 기준일 | 2026-09-30 종가 | C4 |
| 집행일 | 2026-10-01 | C4 |
| 손실 한도 | 원금 대비 −25% | 정의 5 |
| 신호 | 0개 (D4·D5 칸 비움) | 정의 6 |
| 첫 회 국면 적용 | **첫 재구성일에도 그대로.** 09-30 종가가 MA200 아래면 시작부터 노출 50%(종목 수 50 유지) | 사용자 2026-09-20 |
| 첫 회 집행 분할 | **안 함.** 50종목을 집행일 하루에. 미체결은 다음 거래일 1회 재주문 | 사용자 2026-09-20 |

## 3. 정해야 할 것 (구현 전)

| # | 질문 | Claude 권고 | 이유 |
|---|---|---|---|
| Q1 | 모의 체결을 **KIS 모의계좌 발주**로 할까, **자체 가상체결**로 할까 | **KIS 모의계좌** | 시스템 정의 R4 "모의와 실계좌가 같은 코드". 실매매 전환을 빨리 할 수도 있다는 사용자 판단과 맞음. topn 정리로 계좌가 빈다. C9 의 "브로커 발주 금지" 는 C1~C12 계약 조건이고, 새로 만드는 이번 로직에서는 가져다 쓸지 말지 고르는 블록이에요 |
| Q2 | 모의계좌 1억 중 전략 자본만 쓰는 방법 | 전략 자본을 설정값(**09-17 부터 6,000만원**)으로 고정하고 목표 금액을 그 기준으로 계산 | 계좌 잔고와 분리. 나머지는 쓰지 않음 |
| Q3 | v41.1 루프와 계좌 공유 | v41.1 브로커 매수는 SKIP 규칙에 막혀 있어 현재는 충돌 없음. **09-28 가부 때 재확인** | 규칙이 바뀌면 같은 계좌에 섞여요 |

Q1 은 사용자 결정이에요. 나머지는 구현 중 확인.

**Q1 결정 (2026-09-16 22:11, 사용자 "kis"): KIS 모의계좌 발주.**
- 따라오는 것: 체결 귀속은 **주문번호로만**. 같은 계좌를 v41.1 루프가 쓰므로 Q3 재확인이 09-28 가부 기준에 들어가요
- ~~따라오는 것: 시장가 매수 금지 → 지정가~~ **[정정 22:5x] 지정가는 사용자 결정이 아니라 Claude 제안이에요 — 사용자 확인 필요.** 근거: 한투 시장가 매수의 상한가 증거금(2026-07-29 실측), C9 계약(가져다 쓸 후보 블록일 뿐 채택 아님)
- **[09-17 09:4x 결정] 주문 방식 = (가) 시장성 지정가** — 사용자 "가로 진행해". 매수는 그 순간 매도호가1, 매도는 매수호가1. 설명에 같이 드린 C9 규칙(호가 5초 이내, 남은 수량은 다음 거래일 1회 재주문)은 **E 칸 구현 때 게이트로 확인할 블록**이에요 — 결정에 포함됐다고 보지 않아요. 비교했던 (나) 종가 버퍼 지정가(topn 방식), (다) 한투 최유리지정가(증거금 동작 미확인)는 채택 안 함
- 주문 발생 시점(정의 6·8): **분기 재구성일 + KOSPI200 MA200 전환일.** 건수는 첫 재구성 약 50건, 이후 목표−보유 차이분만. ("분기에 한 번 50건" 은 부정확한 서술이었어요)
- 따라오는 것: 발주 도구의 순서 역전(체결 기록으로 주문 파일 생성)과 페이퍼 경로 판정 불일치(09-16 시험)를 **가져오지 않아요.** 새 로직은 주문 → 발주 → 체결 조회 순서로만 가요

## 4. 일정 (거래일 기준, holidays.json 확인: 09-24·25 휴장, 09-19·20·26·27 주말)

| 날짜 | 할 일 | 끝났다는 증거 |
|---|---|---|
| **09-17(목)** | ① topn 6종목 정리(`--apply`, 장중) — **잔고 조회·주문 6건·체결 확인의 호출별 응답 시간과 실패를 기록 = KIS 모의 서버 시험** ② **16시 이후 사용자가 KRX 수동 3종(기본정보·지정 현황·시세) 다운로드** → 형식 대조 → D1·D2 칸 시작 | ① 체결·잔고 0 + 호출 기록 ② 세 파일 열·행 수·해시 |

대시보드는 10-01 첫 재구성 검증에 필요 없어요(검증은 파일로). 새 로직 표시는 10-01 이후.
단 **지금 화면이 틀린 것 2건**(종결된 topn 패널이 진행 중처럼 표시될 가능성, 08:50 상태 알림의 멈춘 리밸런싱 장부 "보유 514종목")은 목록에 두고 10-01 이후 정리. 기록처 RootB `E:\vibe\buffett\PLANS.md`.
| 09-18(금) | D1 입력(종가·상장주식수·KOSPI200) 칸 6줄 + 자료 경로 확정. D2 결과로 대체 경로 판단 | 칸 문서 + 09-17 기준 실제 자료로 로드 |
| 09-21(월) | D3 국면 · D6 선정 · D7 목표 포트폴리오 순수 계산 + 고정 입력 재현(손계산 대조) | 09-18 종가 기준 목표 50종목·금액·수량 표 |
| 09-22(화) | E1 주문 생성(목표 − 보유) + Q1 경로의 주문 한 건 시험(모의, 소액) | 주문번호·체결·잔고 대조 |
| 09-23(수) | E2 상태기계(부분체결·미체결·재시도) + E4 원장·NAV + 사고 4종 판정 | 고의 결함 주입 fixture |
| 09-24·25 | 휴장 | — |
| **09-28(월)** | **가부 판정** (5절) | 판정 기록 |
| 09-29(화) | 전체 리허설: 09-28 종가로 선정~주문 생성까지(발주 없음) | 리허설 산출물 |
| **09-30(수)** | 장후: 실제 선정 + 목표 포트폴리오 + 주문 준비 | 목표 파일·해시 |
| **10-01(목)** | 장중: 발주·체결. 장후: 원장·NAV·사고 판정 | E2E 증거, 사고 0건 |
| 10-02(금) | 미체결 1회 재시도(해당 시) + 검증 보고 | 보고서 |

## 4-1. 검증 시점 (09-17 개정 — 실제 진행 기준, 위 표보다 우선)

구현은 09-17 에 E4·실행기까지 끝났어요. 남은 것은 **실제로 돌려 봐야 확인되는 것**뿐이라 날짜를 거기에 맞춰요.

| 언제 | 무엇을 | 명령 / 볼 곳 | 통과 |
|---|---|---|---|
| 09-17 17:2x (완료) | 6,000만원·노출 100%/50% 정수화, 시험 106건 | D7 계산, pytest | 둘 다 OK, 50% 투자액 28,232,400 = 이전 3,000만·100% 와 같음 |
| 09-17 20:10 이후 (자동) | D1~D7 선정일 20260917 전 구간(6,000만원) | `data/plan_runs/20260917_full/` (09-19 scratchpad 에서 복사, sha256 일치 — PLANS 521) | status OK |
| **09-21(월) 16시 이후** | 계좌 점검 — topn 매도대금 결제(D+2) | `check_account --out-dir ...` | `dnca_tot_amt` 약 1억 440만원, status OK |
| **09-22(화) 10:00~11:00** | 1건 왕복 + 취소 시험 | `run_execution_day test-one --code 005930 --out-dir <data	est_runs60922> --cancel-test` | 주문번호·체결 귀속·prpr·수수료율·당일 재사용·보유 복귀·**취소가 조회로 확인** 전부 true, 사고 0 |
| 09-22 장후 | 실측으로 설정 고정 | 수수료율 실측값을 `fee_pct` 에, 취소 필드가 가정과 다르면 E3 수정 + 시험 | 차이 기록 |
| 09-23(수) | 09-22 에서 나온 수정의 재시험(필요 시 같은 명령 1회) | 같은 명령 | 위와 같음 |
| **09-28(월)** | 가부 판정(5절) + 09:20 카나리아가 아직 켜져 있는지(켜져 있으면 발주 금지 구간 유지) | `Get-ScheduledTask VIBE_Order_Canary_0920` | 5절 전부 충족 |
| 09-29(화) 장후 | 재구성 리허설(발주 없음) — 09-28 KRX 3종으로 D1~D7 → `rebalance --phase SELL/BUY --no-submit` | 리허설 산출물 | 주문 약 50건, 합계 ≤ 6,000만원, STOP 없음 |
| 09-30(수) 20:10 이후 | 실제 선정 | `run_rebalance_plan --selection-date 20260930` | status OK |
| **10-01(목)** | 발주 → sync → 15:20 cancel → sync → ledger → **post-exec-check(비중 판정은 이때 한 번)** | RUN 명세 | 사고 0 |
| 10-02(금) | 전량 매도 경로 리허설: 상태 폴더를 **시험 폴더로 복사** + 가짜 래치(10-01) → `liquidate --no-submit` | 시험 폴더 | 보유 전 종목 매도 주문, 수량 = 원장 |

## 4-2. 10-01 이후 일일 운용 (09-19 추가 — 사용자 지적 "가장 치명적인 공백")

**사용자 결정 (2026-09-19):**
- 4단계(일일 운용) 배치와 OS 스케줄러 등록을 **09-28 가부 판정 전, 09-23~09-27 주간에 끝낸다.**
- **10월 운용은 B1(판단 층을 비운 규칙 기반 바구니) 단독으로 고정한다.** 유효한 알파 가설이 없고, 섞으면 추적오차 원인을 가를 수 없다. 5-5 판단 빈도·5-6 자본 배분은 미결정으로 둔다. D5 후보 분류(PLANS 513)는 실행 경로에 붙이지 않은 상태를 유지하고, H1 수집은 기록만 한다.

**달력 제약 (holiday_manager 실측):** 09-23(수) 다음 거래일은 09-28(월). 09-23~27 주간의 거래일은 **09-23 하루**뿐이라 실제 계좌로 장후 작업을 확인할 수 있는 날은 09-22 저녁·09-23 저녁 두 번이에요. 10-02(금) 다음 거래일은 10-06(화).

**매일 돌 것 (포지션을 가진 다음 날부터):**

| 언제 | 무엇 | 있는 부품 | 없는 것 |
|---|---|---|---|
| 거래일 20:10 이후 (지수 20:05 수집 뒤) | ① `sync` 체결 반영 ② `ledger` NAV·원장 기록 + −25% 래치 판정(`evaluate_latch`, 켜지면 자동 해제 없음) ③ D3 국면 판정(`compute_regime`, t 종가 → t+1 효력) ④ 다음 거래일 할 일 판정: 없음 / 노출 변경 / 래치 청산 → 파일 하나로 남김 | ①② `run_execution_day sync·ledger`, ③ `targets.compute_regime` | ④ 판정기, **분기 중간 노출 변경용 목표 생성**(직전 선정 바구니 × 새 노출 비율, 현재 가격으로 수량 재계산), 저녁 배치 |
| 다음 거래일 10:00 (할 일이 있을 때만) | 래치 → `liquidate` / 노출 변경 → `rebalance SELL` → `rebalance BUY` → 15:20 `cancel` → `sync` → `ledger` → `post-exec-check` | 전부 있음 | 아침 배치(할 일 파일 읽고 분기) |
| 매일 | 예정된 작업이 안 돌았으면 사고 2·3번(조용한 실패·무단 정지)으로 잡고 알림 | `incidents.silent_failures`·`unauthorized_halts` | 연결, 알림 경로 |

**끝났다는 증거 (09-28 가부 기준 8번):**
1. 판정기 fixture 시험 통과: 국면 전환(위→아래·아래→위), 래치 발동, NAV 불완전 시 "판정 불가"(세 번째 값), 지수 파일 낡음, 할 일 없음
2. 저녁·아침 배치가 예약 작업으로 등록되어 있고, **실제 예약 작업이 스스로 한 번 이상 실행된 기록**(TaskScheduler 이벤트 100/102 + 산출물)
3. 09-22 또는 09-23 저녁 실제 계좌로 저녁 배치 1회 `status OK`(포지션 0 상태 → NAV = 자본, 래치 꺼짐, 할 일 없음)
4. 아침 배치는 `--no-submit` 으로 할 일 파일 3종(없음·노출 변경·래치) 각각 분기 확인

**확정 (2026-09-19 사용자 "진행해"):**
- **노출 변경 = 목표 비중 스케일링.** 새 목표 비중 = 선정일 `basket_weight` × 새 노출, 수량 = 원장 NAV × 비중 ÷ 집행 호가(D7 과 같은 정수화, E1 이 이미 함). 입력은 직전 선정 목표 파일 + 원장 NAV + 계좌 호가뿐, KRX 파일 안 읽음. 함수 `scale_current_target`.
  - 보유 수량 ×0.5/×2.0 은 채택 안 함 — 09-17 목표로 계산: 1주 종목 때문에 7종목이 0주로 빠지고, 되돌리면 24종목 수량 불일치·855만원(자본 14%) 미투자
  - 대가: 가격 변동으로 틀어진 비중이 선정일 비중으로 돌아가 전 종목에 소량 거래가 생긴다
- **주문 ID 결함 수리:** 지금 `rebalance_id = 전략|선정일` 이라 같은 선정 바구니의 노출 변경 주문이 재구성 주문과 같은 ID 가 된다(중복 방지로 걸러질 수 있음). 노출 변경은 `전략|선정일|SCALE|노출|효력일` 로 분리. 분기 재구성 ID 는 그대로
- 노출 변경 집행 시각: 다음 거래일 10:00 (재구성과 같은 경로). 알림: 기존 `tools/notify_channels.send_alert`
- **자동 발주 무장은 별도 승인.** 아침·오후 작업은 `config/daily_ops_v1.json` 의 `auto_submit=false` 로 등록 = 판정·계획만 하고 보내지 않음(그림자). 첫 노출 변경이 가능한 날은 10-02 아침이라, **무장 승인은 10-01 저녁 전**에 받아야 한다
- 10-01 절차에 한 줄 추가: post-exec-check 뒤 `daily_ops set-current` 로 현재 목표를 등록(이게 없으면 저녁 판정이 "활성 목표 없음" 으로 아무것도 안 한다)

**예약 작업 3개 (월~금, 휴장일은 스스로 STANDBY 종료):**
| 작업 | 시각 | 하는 일 |
|---|---|---|
| 저녁 | 20:20 (지수 20:05 수집 뒤) | sync → ledger(NAV·래치) → 국면 → 다음 거래일 할 일 판정 → `next_action.json`, `daily_log.jsonl`. 전날 저녁 기록이 없으면 사고 3번 알림 |
| 아침 | 10:00 | 오늘 날짜의 할 일만: 래치 → liquidate / 노출 변경 → SELL → 체결 대기 → BUY. 결과를 `pending_target.json` 으로 |
| 오후 | 15:25 | pending 이 있을 때만: cancel → sync → ledger → post-exec-check → OK 면 현재 목표로 올림(실패면 안 올려서 다음 저녁이 다시 판정) |

**검증 지점 (구현 전 고정):**
| # | 무엇 | 어디서 | 기대값 | 언제 |
|---|---|---|---|---|
| a | 새 시험 + 기존 V2 시험 | pytest | 전부 통과(기존 181) | 구현 직후 |
| b | 분기 재구성 주문 ID 불변 | 시험 | `KOSPI_MCAP_QUARTERLY_V2|20260930|005930|BUY|<tv>|1` 그대로 | 구현 직후 |
| c | 노출 변경 주문 ID | 시험 | `|SCALE|0.50|<효력일>` 포함, 재구성 ID 와 다름 | 구현 직후 |
| d | 저녁 작업 자체 실행 | TaskScheduler 이벤트 100/102 + `data/state/daily_log.jsonl` | status OK, action NONE, 사유 NO_ACTIVE_TARGET, NAV 60,000,000, 래치 꺼짐 | **09-22(화) 20:20** — 거래일 확인함 |
| e | 아침 작업 자체 실행 | 이벤트 102 + 로그 | 오늘 할 일 NONE → rc 0 | **09-23(수) 10:00** |
| f | 오후 작업 자체 실행 | 이벤트 102 | pending 없음 → rc 0 | **09-23(수) 15:25** |
| g | 아침 분기 3종 | 시험(가짜 브로커) | NONE=발주 0 / SCALE=SELL 뒤 BUY 계획 / LIQUIDATE=전량 매도 계획, 전부 `auto_submit=false` 에서 발주 0 | 구현 직후 |

**진행 (2026-09-19 14:5x, PLANS 525):**
- 구현: `src/scale_target.py`(신규), `src/daily_ops.py`(신규: evening·morning·afternoon·set-current), `orders.plan_orders(event_tag=)`, `rebalance_phase` 가 summary 의 event_tag 전달, `config/daily_ops_v1.json`(auto_submit=false), `run_v2_daily_ops.bat`
- **a·b·c·g 통과**: 새 시험 27 + 기존 V2 = **208 passed**
- 배치 실행: 토요일이라 STANDBY rc 0 (정상). 실제 모의계좌 조회 스모크(임시 폴더, 장 열림 강제): NAV 60,000,000 / 래치 기준 45,000,000 꺼짐 / 국면 INDEX_NOT_READY(토요일, 정상) / NONE·NO_ACTIVE_TARGET
- 예약 작업 3개 등록(월~금, StartWhenAvailable, 1시간 제한, IgnoreNew). **첫 실행 09-21(월) 10:00 / 15:25 / 20:20** — d·e·f 는 09-21 부터 확인 가능(계획보다 하루 이름). 정의 XML `backup/20260919_v2_daily_ops/`
- 남은 것: d·e·f 자체 실행 확인(09-21·22·23), **자동 발주 무장 승인(10-01 저녁 전)**

**감시 도구가 V2 를 소유자로 인식 (09-19 사용자 "일정에 넣고 진행해", 원래 09-23~27 일정 → 09-19 완료, PLANS 531·532):**
- 문제: 10-01 뒤 `tools/broker_ledger_reconcile.py` 가 V2 50종목을 미귀속·GAP 으로 매일 경보(경보 피로)
- 수리: V2 `data/state/fills.jsonl` 순보유를 내부 원장으로 추가(v41.1 원장과 겹치지 않으니 더함), 소유자 `kospi_mcap_quarterly_v2`, 둘 이상 주장하면 AMBIGUOUS
- 시험: 감지기 6(정상 침묵 2 / V2 주식이 줄면 GAP / 미귀속 / v41.1·V2 동시 주장 / 원장 깨짐) + 기존 격리 3 = 9 passed. 실계좌 격리 실행 MATCH, V2 종목 0, 공유 산출물 불변
- 확인: **09-20 08:30** `STOC_FullAuto` 가 도는 원장 대조의 `2_Logs/broker_ledger_reconcile_latest.json` 에 `sources.v2_positions` 가 나오면 배선 확인. 10-01 장후 첫 실제 보유로 `ownership` 이 전부 `kospi_mcap_quarterly_v2` 인지
- 남은 것(별개 결함, 이번 범위 밖): 엔진 DDM 이 보는 자산이 옛 값(3,342,301원, 낙폭 96.9%, 단계 4)으로 멈춰 있음(PLANS 519 에서 이미 기록). 청산 대상은 v41.1 open_pos(0)라 매매 영향 없음

## 4-3. 10-01 뒤 코드 정리 (리팩터링) — 09-19 판단: 필요

- **언제:** 10-02 전량 매도 리허설 뒤 첫 주(10-06~). 10-01 전에는 결함만 고친다(PLANS 534)
- **전제:** 시험이 안전망이다(09-19 기준 237 passed). 항목마다 **동작 불변**을 시험으로 먼저 고정하고 바꾼다 — 한 번에 한 항목, 항목마다 전체 시험
- **목록과 기법**(`docs/references/V2_CODE_READABILITY_REVIEW_20260919.md` 4절, 마틴 파울러 『리팩터링』 기법 이름):

| 순 | 정리 | 기법 |
|---|---|---|
| 1 | D7·E1 정수화를 한 함수로 | 함수 합치기 |
| 2 | `plan_orders`(인자 12·분기 26) 쪼개기 | 함수 추출하기, 매개변수 객체 만들기 |
| 3 | 실행기 "정하기"(순수)와 "하기"(발주) 분리 | 단계 쪼개기 |
| 4 | 주문 ID 조립·해석 한 곳 | 기본형을 객체로 바꾸기 |
| 5 | 보유 수량 계산 한 곳 | 함수 합치기 |
| 6 | 이름(용어집 6.2)·KIS 코드값 상수 | 이름 바꾸기, 매직 리터럴을 상수로 |
| 7 | 단언(장부 filled_qty = 체결 합, E1 예산 사후 조건) | 단언 추가하기 |
| 8 | **RootB 살아 있는 도구 3개의 RootA 절대경로 24곳 → `root_config`** (build_dashboard_state_v2 3 / preopen_dashboard_health_check 17 / preopen_dashboard_auto_repair 4). 쓰기는 0건이라 지금은 크게 실패할 뿐 조용히 틀리지 않는다 | 이름 바꾸기·상수화 |
| 9 | **안 도는 RootB 옛 도구 정리**(1_Data 참조 113개 중 살아 있는 6개 외) — 격리 폴더로 이동, 삭제 아님 | — |

- **하지 않는 것:** 동작을 바꾸는 수정은 이 목록에 넣지 않는다(그건 결함 수리로 따로)

## 5. 09-28 가부 기준

**간다 — 전부 충족:**
1. KRX 수동 3종 파일(기본정보·지정 현황·시세)로 D1·D2 가 **재현**된다(09-17 이후 받은 파일 기준)
2. D6·D7 이 고정 입력에서 손계산과 일치한다
3. Q1 경로로 주문 1건 이상이 접수·체결·잔고까지 이어졌다
4. 사고 4종 중 1번(의도≠결과)을 기계가 잡는 fixture 가 통과한다
5. 모의계좌에 topn 잔여 보유가 없다
6. (09-17 추가) 취소 1건이 조회로 확인됐다(09-22 `--cancel-test`) — 안 되면 E3 취소 판정을 고친 뒤 재시험
7. (09-17 추가) 계좌 `nrcvb_buy_amt` ≥ 전략 자본 6,000만원
8. (09-19 추가) 일일 운용(4-2절) 증거 4개 충족 — 판정기 시험 / 예약 작업 자체 실행 기록 / 실제 계좌 저녁 배치 1회 OK / 아침 배치 분기 3종 `--no-submit`
9. (09-19 추가) **v41.1 청산 전용 유지 + v41.1 보유 0** — v41.1 매도 검사가 계좌 전체 수량만 봐서, v41.1 이 포지션을 잡으면 V2 주식을 팔 수 있다(PLANS 528). 확인 네 곳(09-19 실행해 봄: 전부 통과):
   - `run_intraday_paper.bat` 18행 `if not defined PAPER_EXIT_ONLY set "PAPER_EXIT_ONLY=1"` / `run_paper_daily.bat` 128행 `if "%PAPER_EXIT_ONLY%"=="" set "PAPER_EXIT_ONLY=1"`
   - 사용자·시스템 환경변수 `PAPER_EXIT_ONLY` 비어 있음 (`[Environment]::GetEnvironmentVariable('PAPER_EXIT_ONLY','User')`, `'Machine'`)
   - `2_Logs/intraday_loop_status_latest.json` 의 `switches.raw.PAPER_EXIT_ONLY == "1"`, `switches.exit_only_mode == true` (당일 generated_at)
   - `paper/paper_state.json` 의 `open_positions` 길이 0

**판정 방법 (2026-09-20 추가 — 기준은 안 바꿔요, 재는 방법만 못 박아요):**
```
python paper/strategies/kospi_mcap_quarterly_v2/src/go_nogo.py
```
기준마다 **읽은 파일 경로**와 함께 `PASS / FAIL / UNKNOWN` 을 내요. 규칙 셋:
**① UNKNOWN 은 통과가 아니에요**(확인 안 된 것) **② 증거에 나이 한도가 있어 낡으면 UNKNOWN**
**③ 검사가 터져도 UNKNOWN**. 전부 PASS 일 때만 rc=0 이고 "간다" 가 나와요.
Claude 의 산문 보고가 아니라 이 출력으로 판정해요 — 2026-09-20 에 부분 실행(246건)을 전수(711건)로
보고한 적이 있어서, 되돌릴 수 없는 문 앞에서는 사람의 문장을 증거로 쓰지 않기로 했어요.

**2026-09-20 첫 실행 결과: PASS 3 / FAIL 0 / UNKNOWN 6** — 1·4·9 만 통과.
UNKNOWN 6 중 4건은 아직 안 한 일(3·6 은 09-22 실발주, 5·7 은 09-21 계좌 점검, 8 은 09-23~27 일일 운용)이고,
**2번은 성격이 달라요 — D7 status=OK 이지만 기계가 읽을 손계산 대조 기록(`handcalc_*.json`)이 없어요.**
사람이 "맞다" 고 한 것은 여기서 증거가 되지 않아요. 09-21 손계산 대조 때 그 파일을 남겨야 해요.

**미룬다 → 12-30 선정 / 2027-01-04 집행:** 하나라도 미충족. 미루는 건 기준대로의 결정이지 실패가 아니에요.

## 6. 가져다 쓸 후보 (미리 채택 아님 — 칸 구현 때 게이트로 확인)

| 칸 | 후보 블록 | 확인할 것 |
|---|---|---|
| D1 | 가격 패널 로더, `2_Logs/index_daily_history.csv`(2001=KOSPI200, 읽기만) | 휴장일 close=0 패딩, 상장주식수 시점 |
| D1·D2 | **사용자 KRX 수동 다운로드 3종** — 이전 파일: 전종목 기본정보 `paper/strategies/kospi_mcap_quarterly_v1/data/inbox/raw/krx_mdcstat019_all_20260915_154441.csv`(09-15 15:44, 주식종류·시장구분·상장주식수) / 전종목 지정 현황 `_cache/manual_static_population_source/data_4446_20260715.csv`(매매거래정지·관리종목·정리매매 O/X) / 전종목 시세 `_krx_manual/_inbox/krx_price_20260821.csv`(종가·시가총액) | 형식 변화, 받은 시각(종가 확정 후), 파일명 날짜 ≠ 공식 기준일일 수 있음 |
| D2(참고) | C8 same-day snapshot authority | 자동 경로. 이번엔 수동 3종으로 충분 — 필요 없으면 안 가져옴 |

**수동 3종 받는 곳 (파일의 Zone.Identifier ReferrerUrl 실측, 22:2x):**
- 기본정보 + 지정 현황(O/X): `https://data.krx.co.kr/contents/MDC/MDI/mdiLoader/index.cmd?menuId=MDC0201020201` — 07-15 `data_4425`(기본정보 18:44:38)와 `data_4446`(지정 현황 18:44:54)이 같은 주소에서 16초 간격
- 시세: 화면 ID `MDCSTAT015`(전종목 시세). 주소는 받은 파일의 Zone.Identifier 로 다음에 확인
- (이번에 불필요) 거래정지 사건 이력: `menuId=MDC02020601`
| D3 | C5 계약 | — |
| D7 | C6·C7 계약(상한 재배분·정수주) | — |
| E1~E3 | `tools/kis_order_client.py`, `kis_order_dispatch_from_exec.py`(지정가·LOB·멱등), `kis_sync_fills_from_api.py` | 09-16 시험의 순서 역전·경로 간 판정 불일치를 가져오지 않기 |
| E4 | C10·C11 계약 | — |
| 달력 | `holidays.json`, `holiday_manager.py` | — |

## 7. 금지

- 성과를 보고 N·상한·MA·노출비율을 바꾸지 않아요 (이번 검증은 흐름)
- 09-28 가부 기준을 09-28 이후에 바꾸지 않아요. 바꾸면 개정 이력에 "그때 관측 결과가 있었나" 와 함께
- `--allow-offhours` 를 믿고 장외에 `--apply` 하지 않아요 (미사용 인자)
- `2_Logs` 전체 나열 금지

## 개정 이력

- **2026-09-20** 2절 고정 조건에 첫 회 국면 적용·첫 회 분할 없음 추가 (사용자 "둘 다 진행해" = 둘 다 규칙대로). PLANS 542

- **2026-09-19 (09-28 전 개정, 관측 결과 없음)** 가부 기준 9번(v41.1 청산 전용·보유 0) 추가 — 사용자 "진행해". PLANS 530

- **2026-09-19 (09-28 전 개정, 관측 결과 없음)** 사용자 지적으로 4-2절(일일 운용) 추가, 가부 기준 8번 추가, 10월은 B1 단독 운용 고정. PLANS 524

- **2026-09-17 17:3x (09-28 전 개정, 관측 결과 없음)** 사용자 "네 제안대로 진행하고 어느 시점에 검증을 하자". 승인된 Claude 제안 4건:
  1. 전략 자본 3,000만 → **6,000만원**, N=50 유지. 근거: 3,000만·노출 50% 는 FAIL_INTEGERIZATION(현금 26.7%, 0주 11%), 6,000만·50% 는 3,000만·100% 와 투자액이 같아 통과(09-17 D7 실측). 실매매 전환 시 자본은 그때 다시 결정
  2. Claude 설정값 유지: 비용 준비금 0.5%, 매수 비용 0.15%, 발주 금지 09:19:30~09:21:30(카나리아 `VIBE_Order_Canary_0920` Ready 확인), 수수료 0.015%(09-22 실측으로 교체), 비중 사고 허용폭 1%p — **판정은 집행 직후에만**
  3. −25% 래치 뒤: **다음 거래일부터 전량 매도, 자동 재개 없음**, 래치 파일 유지
  4. 09-22 에 취소 확인 시험 추가(체결 안 될 가격 1주 → 취소 → 조회)
  - 가부 기준 6·7 추가, 4-1 검증 시점 표 추가
