# 저장장치 이전 계획 — `E:\1_Data` 를 내장 SSD 로

- 만든 날: 2026-09-19
- 상태: **미확정 (PROPOSED)** — 2026-09-19 사용자 정정 "확정 아님". 처음 기록의 "사용자 승인" 은 틀린 표기. 구매·이전 여부와 시점 모두 사용자 결정 전
- 왜: **지금 `E:\1_Data` 는 USB 외장 HDD**(Seagate BUP Slim)에 있어요. 실측 쓰기 약 60MB/s,
  09-17 에 `2_Logs` 조회가 타임아웃으로 멈춘 적이 있어요. **USB 는 끊길 수 있고, 발주 중에 끊기면
  "주문이 나갔는지 모르는 상태" 가 됩니다.**
- 시점(제안): **10-01 첫 재구성 전.** 실매매 기록이 쌓이기 시작하면 옮기기가 번거로워져요

## 현재 상태 (2026-09-19 실측)

| 항목 | 값 |
|---|---|
| 메인보드 | ASUS PRIME A520M-K (M.2 1개 — 이미 사용 중, SATA 포트 여유) |
| C: | WD Green SN350 1TB, 내장 NVMe, 여유 602GB |
| **E: (작업 경로)** | **Seagate BUP Slim 1863GB, USB HDD**, 여유 1744GB |
| D: | Seagate Backup+ Hub 7452GB, USB HDD (백업) |
| RAM | 16GB **한 장**(DIMM_B1, 3200) — 단일 채널 |
| CPU | Ryzen 5 5500GT 6코어 12스레드 — 교체 불필요 |

## 살 것

1. **내장 SATA SSD 2TB** — `E:` 로 쓴다 (M.2 자리가 없어 SATA, 우리 작업엔 속도 차이 없음)
2. **DDR4-3200 16GB 1장** — 32GB 듀얼 채널 (내장 그래픽이라 듀얼 채널 효과가 큼)
3. (선택) UPS — 장중 정전 대비

## 이전 절차 — 순서대로

```
0. 예약 작업 전부 중지 (VIBE_*, OfficeTest_*, H1 수집 포함)  ← 먼저
1. 장중 루프·워치독 종료 확인 (프로세스 목록으로)
2. 새 SSD 장착 → 포맷 → 임시 문자(예: S:)
3. E:\1_Data 전체 복사 (robocopy /MIR /COPYALL /R:1 /W:1, 로그 남기기)
4. 복사 검증: 파일 수·용량 대조, 핵심 파일 sha256 표본 대조
5. 기존 E: 를 다른 문자(예: X:)로 변경 → 새 SSD 를 **E:** 로 변경
6. 예약 작업 재개 전에 수동 점검
     python -c "from holiday_manager import HolidayManager; print('ok')"
     python -m pytest tests/test_kospi_mcap_quarterly_v2_*.py -q
     python -m paper.strategies.kospi_mcap_quarterly_v2.src.check_account --out-dir <임시>
7. 예약 작업 재개 → 다음 배치(08:30 / 18:30)가 정상인지 확인
8. 옛 USB HDD 는 **한 달간 그대로 보관**(되돌릴 길) — 지우지 않는다
```

## 주의

- **드라이브 문자를 반드시 `E:` 로 맞춘다.** 경로가 코드·예약작업·문서 곳곳에 박혀 있어요
- 복사 중 배치가 돌면 파일이 어긋나요 → 0·1 단계를 건너뛰지 않기
- 장중(09:00~15:30)에 하지 않기. 주말이나 장 마감 뒤에
- 끝난 뒤 `AGENTS.md` 경로 계약과 백업 스크립트의 대상 경로를 다시 확인

## 안 하는 것

- CPU·그래픽 교체 (필요 없음)
- 전용선·고급 네트워크 (우리는 초 단위 등급이라 불필요)
- 외장 SSD (지금보다 낫지만 USB 끊김 위험이 남아서 2순위)
