# v41.1 기준선 — P0 (2026-09-16 21:17 기준)

- 절차: `docs/exec-plans/active/20260916_v41_1_component_judgment_procedure.md` P0
- 성격: 읽기만. 코드·설정·원장 변경 0
- P0 종료 조건 5개 중 **1~4 완료, 5(09-17 관측)는 내일** — 그래서 P0 은 아직 끝나지 않았어요
- 파일명: 계획서에는 `_20260917` 로 예정했으나 기준 시각이 09-16 밤이라 `_20260916` 로 썼어요

## 1. 실효 설정 (`paper_engine.config.load_config()` 결과 — 설정 파일이 아니라 실효값)

```
max_gross_exposure_pct     0.01      <- 자본 1억의 1% = 100만원. 09-11 topn 여력 보호용으로 1.0 -> 0.01
max_new_trades_per_day     15
max_positions              18
capital_total              100,000,000
stop_loss_pct              -0.05
max_hold_days              8
min_qty                    1
capital_budget_policy      gross .55 / basic .15 / surge .15 / split .40 / recovery .10 / reserve .20
split_entry                first_ratio 0.3, second 확인 조건 5개
로드 경고                  news_signal_shadow_entry_policy / t2_settlement_cash / defense_signal_entry_policy 기본값 채움(메모리만)
```

**topn 종결(09-16)로 0.01 의 이유가 사라졌어요.**

~~이 값이 수량 축소 사슬의 첫 단계(`budget_cap`)예요.~~ **[정정 21:3x] 틀렸어요 — 추정을 사실로 적었어요.**
`budget_cap` 은 `paper_engine/entry.py:3315` `cap_basic_qty` = **기본 배분 한도**예요.
자본 1억 × `basic_alloc_pct` 0.15 ÷ `basic_target_positions` 18 = **종목당 약 83.3만원**.
실측 대조: 030520 `487>48` → 487×17,100=833만원 → 48×17,100=82만원 / 041460 `2443>244` → 833만원 → 83만원.
`calc_qty` 원값이 항상 약 833만원(=1억/12)이라 이 단계는 **어느 종목이든 ×0.1** 이에요.

그 뒤 단계도 상수에 가까워요(09-15·16 기록 12건):

```
budget_cap          ×0.10   기본 배분 83만원/종목 (surge 경로엔 없음)
split_first_ratio   ×0.30   split_entry.first_ratio
normal_qty_reduce   ×0.50   NORMAL_INTRADAY_MOMENTUM_REDUCE
weight_adjust       ×0.25   position_size_multiplier (8건 전부 정확히 1/4) — 출처 미확인:
                            dd_stop_validation 0.25 / account_clear_min_scale 0.25 / edge_zero_floor_scale 0.25 중 하나
surge_budget_fit    ×0.4~0.5
post_sector_limits  ×0.1~0.5
```

### weight_adjust ×0.25 의 출처 — [21:4x 코드+런타임으로 확정]

`paper_engine.py:642-654` `_compute_risk_orch_scale()` → `position_size_multiplier = min(1, scale)` → `entry.py:3519-3522` 에서 수량에 곱해요.
09-16 15:25 런타임(`2_Logs/p1_entry_gate_status_latest.json` `risk_gate_runtime.risk_orchestration`)으로 산식을 재현하면:

```
c (NORMAL 신뢰도)            0.40
× f_kelly (cap)             1.50    (raw 2.09 = edge 0.00833 / variance 0.00399, 최근 60거래)
× vol_ratio                 0.475   (target_vol 0.03 / est_vol 0.0632)
= 0.285
× 비용 보정 tc_adj           0.735   (edge / (edge + tc_est 0.003))   -> scale_after_kelly_tc 0.2095
dd_taper                    미적용  (mode=advisory; strategy_dd 55.2% 인데 account_dd 2.35% 로 대체)
× es_gate                   0.50    (ES −7.55% ≥ 한도 5%)            -> 0.1048
account_clear_min_scale     바닥 0.25 로 올림                        -> **0.25**
```

- **0.25 는 "줄인 값"이 아니라 "바닥으로 올린 값"이에요.** 계산값은 0.105 였어요. 바닥을 없애면 더 작아져요.
- 입력은 **v41.1 자신의 최근 60거래**예요. 신호가 음(실측)으로 판정된 전략의 거래 통계가 크기를 정해요 — P2 판정 대상.
- `tc_est` 0.003 은 `fee_pct 0.0 + slippage 0.001 + 세금 0.002`. 권위 비용 0.358% 와 달라요(메모).
- `strategy_dd 55.2%` 가 `account_clear_strategy_dd_override` 로 계좌 기준 2.35% 로 바뀌어 dd_taper 를 피하고 있어요 — 기록만.

**노출 상한 0.01 이 어느 단계에서 얼마나 무는지는 아직 안 쟀어요.** 페이퍼 보유 7종목 합계 24만원이라 100만원 상한 아래예요.
그래서 P1-1(0.01 되돌리기)의 금액 효과는 **격리 재실행 전에는 말할 수 없어요.**

`generate_candidates_v41_1.DEFAULT_PARAMS` 게이트:

```
rs_lim 0.05 / v_accel_lim 2.5 / v_accel_max 5.0 / value_min 10억 / atr_max 0.12
stretch_max 1.19 / rsi_max 70 / near_52w_high_gap_max 0.25 / min_listing_days 126
require_macd_golden 0 / junk_risk_enable 1
```

(`stable_params_v41_1.json` 은 루트에 없어요 — 인용 금지 대상이기도 해요)

## 2. 스위치 (실행 중 프로세스 실측)

```
pid 20052  intraday_paper_loop.py --mock auto --interval 2 --max-orders 5 --dispatch-apply
           기동 2026-09-16 08:30:48
PAPER_EXIT_ONLY      0   (run_intraday_paper.bat:15 기본값, run_paper_daily.bat:125 도 0)
KIS_MOCK             1
LOOP_DISPATCH_APPLY  1   <- 배치 파일 기본값은 0(run_intraday_paper.bat:82). **사용자 환경변수(User) = 1** 에서 온다
BROKER_MODE          DRY (run_paper_daily.bat:1396, 일배치 발주 경로)
```

## 3. 원장 해시

```
paper/fills.csv        f8030b6517e93243  09-16 14:04  1063행
paper/trades.csv       42d55f962a7dd32a  09-16 14:04   572행
paper/trades_calc.csv  867dda38a1b785bd  09-16 14:04   661행
```

09-11 이후 페이퍼 체결(행 수): 09-14 BUY 9 / SELL 12, 09-15 BUY 9 / SELL 11, 09-16 BUY 4 / SELL 6.

## 4. O6 판정 — **미달. 세 거래일 모두 발주 칸에서 멈췄어요**

| 칸 | 09-14 | 09-15 | 09-16 | 증거 |
|---|---|---|---|---|
| 후보·진입·orders_exec | O | O | O | `paper/orders_2026091{4,5,6}_exec.xlsx` 존재 |
| 발주(브로커) | **X** apply=False | **X** apply=False | **X** apply=True 인데 전부 SKIP | `2_Logs/kis_order_dispatch_ledger.jsonl` |
| 발주 결과 집계 | DRY_RUN 476, ORDERFLOW_FAIL_CLOSED 109, CONFLICT_SIDE 20 | DRY_RUN 494 | SKIP_ALREADY_PAPER_FILLED 15, DRY_RUN 2 | 같은 파일 |
| v41.1 주문번호 | 0 | 0 | 0 | 09-11 이후 0건 |
| 페이퍼 체결→trades_calc | O | O | O | 위 3절 |
| 통계 | — | — | O `live_vs_bt` PASS as_of 20260916, fills_effective 10 | `E:\vibe\buffett\data\stats\live_vs_bt.json` 16:20 |

**멈춘 이유가 날마다 달라요.**
- 09-14·15: 루프가 발주 스위치 없이 돌았어요. 환경변수가 그 이후 루프 기동에만 적용된 것으로 보여요 — 설정 시각은 **미확인**
- 09-16: 스위치는 켜졌는데 `tools/kis_order_dispatch_from_exec.py:1709-1725` 가
  `mock + apply + BUY + 페이퍼가 이미 그 order_id 로 체결 기록` 이면 `SKIP_ALREADY_PAPER_FILLED` 로 건너뛰어요.
  메시지는 `idempotency_self_skip: paper_engine already wrote fill ... use --force-resend to override`.
  **장중 루프는 페이퍼 엔진이 먼저 체결을 쓰고 그다음 발주를 부르니까, 이 규칙 아래서는 v41.1 BUY 가 모의계좌로 갈 수 없어요.**

**09-11 PREREG 조건 변경("09-14 부터 v41.1 이 같은 계좌에서 소액 실제 발주")은 한 건도 실현되지 않았어요.**

## 5. 미확인 3건

| 항목 | 결과 | 분류 |
|---|---|---|
| `SKIP_ALREADY_PAPER_FILLED` 의도 | 의도된 멱등성 규칙이에요(08-21 감사에서 존재 확인, 사유 메시지 명시). 다만 **mock 에서 페이퍼 엔진을 집행자로 보는 설계**라, O6 정의("발주→체결")와 충돌해요 | 설계 충돌. **P1 후보** |
| 08:50 "보유 514종목 현금 0.0%" | `tools/build_status_digest.py:56-89` `section_book()` 이 **리밸런싱 가상장부** `2_Logs/rebalance/rebal_state.json` 을 읽어요. v41.1 이 아니에요. 그 장부의 예약작업 `VIBE_Rebalance_Morning/Evening` 은 **Disabled(마지막 09-10)**, 자산곡선 마지막 날짜 **20260909**. 기준일은 같이 찍히지만 매일 아침 첫 줄 "장부"로 나와 현재 상태처럼 읽혀요 | 표시 결함 후보(낡은 장부를 현재처럼). 매매 영향 0 |
| topn 잔여 보유 | **[정정 21:2x] 017900 한 종목이 아니라 6종목 전부예요.** 브로커 스냅샷(09-16 10:51)과 `2_Logs/topn/positions.csv` 가 종목·수량 일치: 000370 2,003 / 001210 1,307 / 003230 12 / 012210 1,624 / 377450 976 / 017900 2,025. 평가 **100,505,260원**, 미실현 +3,688,758원. 모의계좌 사실상 전체예요. 처음 판단은 09-10 에 철회된 "5종목은 v41.1" 을 옮겨 적은 거였어요 | **P1 선행 조건으로 올려요.** 브로커 현금이 약 330만원뿐이라, 노출 상한(1번)을 올려도 v41.1 이 모의계좌로 살 돈이 없어요. 처리안: (가) 그대로 두고 v41.1 은 페이퍼만 (나) hold 13 만기(약 09-29)에 일괄 수동 매도 (다) 지금 매도. 권고는 (나) — 사전등록 청산 규칙 그대로라 결과 해석이 섞이지 않아요. 수동 매도 실행은 승인 필요 |

## 6. 09-16 불안정 (P0-5 관측 대상, 원인 미규명)

- 08:34 아침 배치 FAIL: `resilience_check` `gateway_health` primary age 2.147분 > 2.0분 (latency 44ms 는 정상)
- 장중 루프 스텝 실패 경보: 09:07 / 09:22 / 14:24(`paper_engine` 포함) / 14:38 / 15:07
- 저녁 cycle 21: 10단계 중 8단계 rc=124 (dashboard 605초)
- 10:30 E2E 1/3회 FAIL (발주·취소·상태조회 180초 타임아웃)
- 20:29 `account_snapshot` rc=2 (모의 서버 연결 끊김)

**09-17(목) 볼 것:** 08:30 배치가 같은 사유로 죽는가 / 장중 스텝 실패가 반복되는가 / 새 루프가 `LOOP_DISPATCH_APPLY=1` 로 뜨는가

## 7. P1 로 넘길 후보 (승인 필요, 아직 아무것도 안 바꿨어요)

```
0  topn 보유 6종목(1억)         [21:2x 추가] 모의계좌 현금 ~330만원. 이게 안 풀리면 브로커 발주가 돈 문제로 막혀요
1  max_gross_exposure_pct 0.01   topn 보호 이유 소멸. 되돌리면 노출 증가 — 숫자로 승인
2  SKIP_ALREADY_PAPER_FILLED     mock 발주가 구조적으로 0. O6 정의를 바꿀지 규칙을 바꿀지 결정
3  수량 축소기 곱                 1 을 바꾸면 곱이 달라져요. **1 먼저 결정하고 곱을 다시 재요**
4  gateway_health 2.0분          09-17 재발 여부 본 뒤
```

1·2·3 은 서로 얽혀 있어요. 한 번에 하나 원칙대로 **1 → (재측정) → 3 → 2** 순서를 권해요.
0 은 브로커 경로(2)에만 걸리고 페이퍼 경로(1·3)에는 안 걸려요. 그래서 1·3 은 0 과 독립으로 진행할 수 있어요.

설정 경로 확인(21:2x): `paper/paper_engine_config.json` 옆에 `paper_engine_config.lock.json` 이 있어요 → **직접 편집 금지, 설정 도구로.**
09-11 변경 로그 `2_Logs/paper_engine_config.change_20260911_160323.json`(SET `max_gross_exposure_pct` 0.01), `..._160356.json`.
09-08 에 설정 도구가 리스트를 dict 로 덮어쓴 사고가 있었으니 변경 명령은 격리 경로(`PAPER_CONFIG_PATH`)에서 먼저 시험해요.
