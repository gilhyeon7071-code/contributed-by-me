# 새 로직 용어집

- 작성: 2026-09-17 08:50
- 상태: `CONFIRMED_2026-09-17` — 사용자 "너의 제안대로 진행". **이름 = A: 분기 시총 / `KOSPI_MCAP_QUARTERLY_V2`**, 용어·번호·파일 규칙 제안 그대로 확정
- 적용: 새 로직의 문서·코드 변수·파일 이름·산출물 키
- 규칙: **개념 하나에 단어 하나.** 표의 "쓰지 않는 말" 은 새 로직에서 쓰지 않아요. 다른 로직(v41.1 등)의 기존 문서는 고치지 않아요
- 이유: 2026-09-16 사용자 지적 "로직 내 명칭이 매끄럽지 않지"

---

## 1. 새 로직 이름 — **사용자 결정 필요**

| 후보 | 한글 | ID | 장단점 |
|---|---|---|---|
| A (Claude 권고) | 분기 시총 | `KOSPI_MCAP_QUARTERLY_V2` | 같은 계열임이 드러나고, V1(C1~C12 계약, N=100, C8 과거 복원 작업물)과 폴더·원장이 섞이지 않아요 |
| B | 분기 시총 | `KOSPI_MCAP_QUARTERLY_V1` | 폴더가 이미 있어요. 대신 V1 의 계약(N=100·브로커 발주 금지)과 이번 결정(N=50·KIS 모의 발주)이 한 이름 아래 충돌해요 |
| C | 사용자 지정 | — | — |

## 2. 매매 용어

| 쓰는 말 | 뜻 | 코드 이름 | 쓰지 않는 말 |
|---|---|---|---|
| **재구성** | 목표 포트폴리오를 새로 정하는 정기 행위(분기) | `rebalance` | 리밸런싱, 교체, 회전(회전은 비율 지표로만) |
| **선정일** | 선정에 쓰는 종가의 날짜 (분기 마지막 거래일) | `selection_date` | 기준일, as_of, 형성일, 신호일 |
| **집행일** | 주문을 내는 날 (선정일 다음 거래일) | `execution_date` | 매매일, D, exec_date |
| **모집단** | 선정 대상이 되는 종목 전체 (KOSPI 보통주, 제도 차단 제외) | `universe` | 유니버스, 판, 후보군 |
| **제도 차단** | 거래정지·관리종목·정리매매로 모집단에서 빼는 것 | `regulatory_exclusion` | 상태 플래그, 지정 현황(파일 이름으로만), 게이트, 필터 |
| **선정** | 모집단에서 시총 상위 N 종목을 고르는 것 | `selection` | 후보 생성, 스크리닝 |
| **국면** | KOSPI200 종가와 MA200 의 관계 | `regime` (`above` / `below`) | 레짐, 방어, risk_on/off |
| **노출** | 목표 포트폴리오에 실제로 담는 비율 (100% / 50%) | `exposure` | 비중 축소, 보유 비율 |
| **목표 포트폴리오** | 종목별 목표 비중·금액·수량 | `target_portfolio` | 타겟, 목표 비중표, 바스켓 |
| **비중 상한** | 한 종목 최대 비중 20% | `weight_cap` | 캡, 상한 재배분(과정 이름으로만) |
| **전략 자본** | 이 로직이 쓰는 금액 ~~3,000만원~~ **6,000만원**(09-17 개정) (계좌 잔고와 별개) | `strategy_capital` | 원금, 자본, 계좌 자산 |
| **주문** | 목표 − 보유로 만든 매수·매도 지시 한 건 | `order` | 의도, intent, 오더 |
| **발주** | 주문을 KIS 로 보내는 행위 | `submit` | 전송, 디스패치, dispatch |
| **체결** | KIS 가 확인한 실제 거래 | `fill` | 가상 체결, 페이퍼 체결 |
| **모의** | KIS 모의계좌로 실제 발주하는 운영 방식 | `mock` | 가상매매, 페이퍼, 시뮬레이션 |
| **원장** | 현금·보유·체결·비용 기록 | `ledger` | 장부, 포지션 파일 |
| **NAV** | 전략 자본 기준 평가 금액 | `nav` | 자산, 에쿼티 |
| **사고** | 의도와 결과가 다른데 시스템이 스스로 모른 경우 (4종, 시스템 정의 R10) | `incident` | 오류, 실패(실패는 사고가 아닐 수 있어요) |

## 3. 뼈대·검증 용어

| 쓰는 말 | 뜻 | 쓰지 않는 말 |
|---|---|---|
| **층** | 판단 층 / 집행 층 / 평가 층 | 1층~5층(뼈대 v1), 레이어 |
| **칸** | 층 안의 한 자리 (D1~D7, E1~E4, V1~V2) | 역할, 단계, 스텝 |
| **게이트** | "검증됐다" 고 말하기 위한 확인 G1~G8 | 관문, 체크 |
| **흐름 검증** | 선정→목표→주문→발주→체결→원장이 정의대로 한 번 도는지 | E2E(설명할 때만), 운영 반영 |
| **성과 평가** | 수익·위험 판단 (첫 재구성 뒤 별도) | 검증(성과 뜻으로) |
| **확인된 사실** | 기존 로직 검증에서 나와 그대로 쓰는 사실 | 교훈, 재료 |
| **블록** | 기존 코드·계약 중 칸에 가져다 쓰는 것 | 부품, 재사용 |

## 4. 번호 체계 — 넷만 써요

| 번호 | 가리키는 것 | 문서 |
|---|---|---|
| `D1~D7` `E1~E4` `V1~V2` | 뼈대 칸 | `TRADING_LOGIC_SKELETON_20260916.md` |
| `G1~G8` | 게이트 | 같은 문서 |
| `C1~C12` | 기존 계약 항목 — **블록 출처로 인용할 때만** | `STRATEGY_TARGET_CONTRACT_RECONCILIATION_20260914.md` |
| `R1~R22` | 시스템 정의 요구 — **인용할 때만** (R5 전환 조건, R10 사고 등) | `SYSTEM_DEFINITION.md` |

**새 로직에서 쓰지 않는 번호:** M0~M8, F0~F10, P0~P6, A0~A5 (보류·종료된 계획), 뼈대 v1 의 R 역할, 결정 질문 Q·가~바 (그 문서 안에서만).

## 5. 파일·폴더 이름 규칙 (제안)

- 폴더: 1절에서 정한 ID 의 소문자 (`paper/strategies/<id 소문자>/`)
- 날짜: 파일 이름의 날짜는 **선정일** 기준 `YYYYMMDD` — 받은 날·만든 날과 섞지 않아요. 만든 시각은 파일 안에 적어요
- 결정 근거가 되는 기록은 날짜 없는 append-only 원장에도 남겨요 (날짜 붙은 산출물 30일 정리 대비)
- 인코딩: UTF-8. 만들 때마다 `run_scan_mojibake_text.bat` 로 확인 (G7)

## 6. 2026-09-19 추가 — **제안, 사용자 확인 전**

일일 운용(exec plan 4-2)과 독립 검토 수리에서 생긴 개념, 그리고 코드 이름과 용어집이 어긋난 곳이에요.
코드 이름은 **10-01 첫 집행 전에는 바꾸지 않아요**(시험·09-22 확인 상태를 흔들지 않으려고). 대응만 적어 두고 10-01 뒤 정리해요.

### 6.1 새 용어
| 쓰는 말 | 뜻 | 코드 이름 | 쓰지 않는 말 |
|---|---|---|---|
| **노출 변경** | 국면이 바뀌어 같은 선정 종목의 노출만 바꾸는 것(종목 교체 아님) | `SCALE`, `scale_current_target` | 스케일링, 비중 축소, 리밸런싱 |
| **바구니 비중** | 노출을 곱하기 전, 선정 종목끼리의 비중(합 1) | `basket_weight` | 바스켓(이 뜻 말고는 쓰지 않음) |
| **계좌 목표 비중** | 바구니 비중 × 노출 | `account_target_weight` | 타겟 비중 |
| **현재 목표** | 지금 따르고 있는 목표 포트폴리오(파일 쌍 + 노출) | `current_target` | 활성 바스켓 |
| **할 일** | 저녁에 정하는 다음 거래일 행동: 없음 / 노출 변경 / 래치 청산 / 판정 불가 | `next_action` | 신호, 트리거 |
| **래치** | 전략 NAV 가 전략 자본의 75% 이하가 되면 켜지고 자동으로 꺼지지 않는 멈춤 | `latch` | 킬스위치, 서킷브레이커, DDM |
| **래치 청산** | 래치 다음 거래일부터 보유 전량 매도 | `liquidate` | 강제 청산, 손절 |
| **재주문** | 전날 남은 수량을 다음 거래일 1회 다시 내는 것 | `replay` | 리트라이, 재전송 |
| **그림자 모드 / 무장** | 계획·기록만 하고 발주 안 함 / 실제 발주함 | `auto_submit=false` / `true` | 드라이런(그 뜻으로), 셰도우 |
| **평가일** | 원장·NAV 를 계산한 날짜 | `as_of` (원장에서만) | 기준일 |

### 6.2 코드 이름 ↔ 용어집 어긋남 (10-01 뒤 정리 목록)
| 코드 | 용어집 | 어긋난 점 | 정리안 |
|---|---|---|---|
| `intent_id` (37곳) | 주문 = `order`, "intent" 금지 | 코드의 `intent_id` 가 용어집의 **주문**이고, 코드의 `order_id`(= intent|시도번호)는 **발주 시도** 한 번이에요 | 용어집에 "주문(`intent_id`) / 발주 시도(`order_id`)" 로 대응을 적거나, 코드를 `order_key` / `attempt_id` 로 |
| `strategy_equity` (orders.py, 6곳) | NAV, "에쿼티" 금지 | 뜻은 NAV | `nav` 로 |
| `rebalance_phase`, `rebalance_id` | 재구성 = 분기 정기 행위 | 노출 변경·재주문도 같은 함수로 집행해요 | 집행 함수는 `execute_target_phase`, ID 는 그대로(주문 ID 호환) |
| `basket` (11곳) | "바스켓" 금지(목표 포트폴리오 뜻) | 코드는 다른 뜻(바구니 비중)으로 써요 | 6.1 **바구니 비중**으로 정의 — 이름은 유지 |
