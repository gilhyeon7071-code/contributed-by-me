# 저장소 운영 계약

## 1. 작업 범위
- RootA = `E:\1_Data`
- RootB = `E:\vibe\buffett` (데이터 빌더 · 상태 JSON · PLANS) + `E:\vibe\control_center_v2` (Control Center V2, 현재 사용 중인 대시보드 UI)
  - 두 경로는 한 시스템이다. `E:\vibe\buffett\tools\build_dashboard_state_v2.py`가 상태를 만들고, `E:\vibe\control_center_v2`(React/Vite)가 `runs\dashboard_state_latest.json`을 읽어 화면에 그린다.
  - `E:\vibe\buffett\react_forensic_dashboard` 와 `E:\vibe\buffett\pages` 는 현재 라이브가 아니다 (2026-08-20 확인). 대시보드를 찾을 때 이쪽을 뒤지면 헛짚는다.
- `D:\` 는 백업 확인 외 사용 금지
- 예외: 승인된 2차 백업 루트 `D:\1_Data_Offsite_Backup` 에 한해, `E:\1_Data\tools\maintenance\offsite_backup_manifest.py`의 dry-run 검증 후 사용자가 명시 승인한 경우에만 쓰기를 허용한다.
- 명시적으로 지정된 파일만 다룰 것
- 범위를 임의로 확장하지 말 것

## 2. SSOT 체인
- `orders(D) -> fills(D) -> ledger -> stats`

## 3. D 규칙
- `D = fills.csv` 에서 최신 `BUY ymd`
- `BUY`가 없으면 `datetime` 앞 8자리 기준 최신 `ymd` 사용

## 4. STOP 조건
다음 중 하나라도 발생하면 즉시 중단해야 한다.
- `orders_exec` 없음
- `exec_date != D`
- `as_of / run_id` 불일치
- `paper / broker` 날짜 혼용

## 5. 안전 원칙
- FAIL-CLOSED 원칙 유지
- Gate 의미 변경 금지
- 명시적 요청 없이 정책 의미 변경 금지
- 명시적 요청 없이 LOCK 의미 변경 금지
- 코드에 존재한다는 이유만으로 완료로 판단하지 말 것

## 6. 필수 검증 항목
아래 6개 검증 항목이 모두 명시적으로 확인되고 보고되지 않으면 작업은 완료가 아니다.
1. 기능 검증
2. 정합성 검증
3. 운영 반영 검증
4. 정책 검증
5. FAIL-CLOSED 검증
6. 회귀 검증

## 7. 완료 판정 규칙
- 아래 6개 검증 항목이 모두 PASS이고, 증거가 있을 때만
  `완료`, `수정 완료`, `문제없음`, `운영 가능`, `ready`, `done`, `fixed`
  라고 쓸 수 있다.
- 미검증 항목은 숨기지 말고 반드시 `FAIL` 또는 `NA`로 표시할 것
- 코드 확인만으로는 충분하지 않다
- 런타임 증거가 반드시 필요하다

## 8. 보고 규칙
항상 아래를 분리해서 작성할 것
- 사실
- 해석

항상 아래를 포함할 것
- 증거 경로
- 확인한 핵심 값
- 무엇을 테스트했는지
- 무엇을 테스트하지 않았는지
- 검증 항목별 `PASS / FAIL / NA`

## 9. 작업 절차
- 복잡한 작업 또는 큰 리팩터링은 코딩 전에 실행 계획(ExecPlan)을 먼저 작성하거나 갱신할 것
- 정책, 배치 엔트리포인트, SSOT 체인, D 규칙, FAIL-CLOSED 동작에 영향을 주는 변경은 계획 문서 작성이 필수다
- 코드 수정 후에는 관련 검증을 실행하고, 증거를 정리한 뒤 최종 판정을 내릴 것

## 10. 추가 강제 규칙
다음 중 하나라도 해결되지 않았으면 자동으로 완료가 아니다.
- `orders_exec` 없음
- `exec_date != D`
- `as_of / run_id` 불일치
- `paper / broker` 날짜 혼용
- 운영 반영 증거 없음
- 회귀 검증 없음

## 11. End-to-End 원칙
- `orders(D) -> fills(D) -> ledger -> stats` 체인에 관련된 작업은
  코드 수준 확인만으로 완료로 볼 수 없다
- End-to-End 증거가 있어야 완료 판정 가능하다

## 문서 경로
- 실행 계획 기준: [`E:\1_Data\.agent\PLANS.md`](E:\1_Data\.agent\PLANS.md)
- 작업 프롬프트 고정 문구: [`E:\1_Data\docs\references\WORK_PROMPT_MANDATORY.md`](E:\1_Data\docs\references\WORK_PROMPT_MANDATORY.md)
- 최종 보고 템플릿: [`E:\1_Data\docs\references\FINAL_REPORT_TEMPLATE.md`](E:\1_Data\docs\references\FINAL_REPORT_TEMPLATE.md)

## 12. 문자열 깨짐 검증 원칙
- 깨진 문자열 의심 시 먼저 `E:\1_Data\run_scan_mojibake_text.bat`로 dry-run 검증한다.
- `--apply`는 명시 파일에만 사용하고, 자동 백업 경로를 확인한 뒤 보고한다.
- 복구 가능한 reversible mojibake 라인만 수정한다.
- 물음표 반복형처럼 원문 복원이 불가능한 문자열은 추정 보정하지 않고 ISSUE로 남긴다.
- 문자열 표시 수정은 정책값, 점수, 게이트 상태 변경과 분리해서 보고한다.

## 13. 파일 인코딩 정책
- 신규 텍스트 파일은 UTF-8을 기본으로 한다.
- JSON/Markdown/Python/CSV 산출물은 UTF-8 또는 UTF-8-SIG를 사용한다.
- PowerShell `.ps1`은 UTF-8-BOM을 허용한다.
- Windows Batch `.bat/.cmd`는 기존 인코딩을 유지한다.
- 기존 파일의 인코딩은 명시 요청 없이 변환하지 않는다.
- `apply_patch`가 파일 인코딩 때문에 실패하거나 읽지 못하는 경우, 해당 파일은 원본 백업 후 PowerShell/.NET 방식으로 동일 인코딩을 유지해 최소 치환한다.
- 인코딩 유지 수정은 일반 코드 수정과 분리해 보고한다.
- 인코딩 변경이 필요하면 정책 변경으로 간주하고 사전 확인 후 진행한다.
- 수정 후에는 파일 파싱/실행 검증과 문자열 깨짐 검증을 수행한다.

## 14. 백업 파일 정책
- 수정 전 백업은 `E:\1_Data\backup\YYYYMMDD_작업명\YYYYMMDD_HHMMSS\` 아래에 둔다.
- 기존 루트 직하위 `backup_*` 파일/폴더는 레거시 백업 산출물로 간주한다.
- 백업 파일은 운영 소스가 아니며, 품질 스캔/문자열 깨짐 스캔의 기본 판정 범위에서 제외한다.
- 백업 내용 확인이 필요한 경우에만 명시 경로로 개별 스캔한다.
- 백업 보존기간은 기본 90일로 한다.
- 백업 삭제는 `E:\1_Data\run_temp_cleanup_policy.bat`의 dry-run 결과 확인 후, 사용자가 명시적으로 승인한 경우에만 `DOIT`로 진행한다.
- 백업 삭제/이동은 운영 로그, 최신 JSON, 히스토리 CSV 보정과 분리해서 보고한다.
- 단일 드라이브 장애 대비용 2차 백업은 `D:\1_Data_Offsite_Backup`만 허용한다.
- 2차 백업 기본 대상은 핵심 실행 코드, 설정/락, 체결/원장/상태, 가격 parquet로 제한한다.
- 2차 백업 대상에는 API 키, 자격증명, 개인 메모 파일을 포함하지 않는다.
- 2차 백업은 기본 dry-run이며, 실제 복사는 dry-run 보고서 확인 후 사용자가 명시 승인한 경우에만 진행한다.
- 2차 백업 apply는 `--apply --confirm-d-drive`가 모두 있을 때만 허용한다.
- 2차 백업 결과는 원본/백업 크기와 SHA256 해시를 보고서에 남긴다.

## 15. PLANS 기록 분류
- RootA(`E:\1_Data`) 매매 로직, 배치, 데이터 생성, SSOT, 주문-체결-원장 작업은 `E:\1_Data\.agent\PLANS.md`에 기록한다.
- RootB(`E:\vibe\buffett` + `E:\vibe\control_center_v2`) 대시보드, 화면, 상태 JSON, React 작업은 `E:\vibe\buffett\PLANS.md`에 기록한다. UI 코드가 `control_center_v2`에 있어도 **기록처는 `buffett` 한 곳으로 유지**한다.
- 두 영역을 함께 수정한 경우 양쪽 PLANS에 각각 해당 관점으로 나눠 기록한다.
- 동일 작업이 RootA와 RootB를 모두 변경하면 하나의 공통 작업명/날짜를 사용하되, RootA PLANS에는 매매·배치·데이터 관점만, RootB PLANS에는 대시보드·상태·화면 관점만 기록하고 서로의 산출물 경로를 참조로 남긴다.
- 사용자가 PLANS 업데이트를 요청하면 범위에 맞는 양쪽 문서를 모두 확인한다.

## 16. 검증 프롬프트 별칭
- 사용자가 아래 별칭으로 검증을 요청하면, 해당 파일을 먼저 읽고 그 기준으로 read-only 감사한다.
- 별칭 검증은 기본적으로 수정 금지이며, 발견사항과 수정 필요 여부만 보고한다.
- 사용자가 명시적으로 수정까지 요청하기 전에는 코드/데이터/정책 파일을 변경하지 않는다.
- 별칭 프롬프트를 적용할 때도 이 AGENTS.md의 STOP 조건, D 규칙, FAIL-CLOSED, 6개 검증 항목을 함께 적용한다.
- 경로 기준: 2026-08-18 확인 결과 실제 위치는 `E:\TMP\검증프롬프트 세부\`다. 이전 `D:\검증프롬프트 세부\` 표기는 존재하지 않는 경로였다.
- 언더스코어 파일명(예: `공통_운영감사_기준.txt`)은 2026-05 개정본 12종이다. 개정본은 공통 블록을 본문에서 제거하고 `공통_운영감사_기준.txt`를 SSOT로 참조하므로, **개정본 별칭을 실행할 때는 `공통_운영감사_기준.txt`를 함께 읽어야 한다.** 개정 경위는 같은 폴더의 `00_검토결과_및_변경내역.md`에 있다.
- `파일 없음` 주석이 붙은 별칭 12종은 현재 원본이 존재하지 않는다. 해당 별칭 요청 시 감사 수행 대신 파일 부재를 먼저 보고한다.
- "뉴스파이프라인 검증" 요청 시:
  `E:\TMP\검증프롬프트 세부\뉴스파이프라인_에디터.txt`
- "일반매매와 급등매매 로직 검증" 요청 시:
  `E:\TMP\검증프롬프트 세부\일반매매와 급등매매 로직검증.txt`
- "일반매매 검증" 요청 시:
  `E:\TMP\검증프롬프트 세부\일반매매 로직검증.txt`
- "급등매매 검증" 요청 시:
  `E:\TMP\검증프롬프트 세부\급등매매_로직검증.txt`
- "ATR 로직 검증" 요청 시:
  `E:\TMP\검증프롬프트 세부\ATR로직_검증.txt`
- "급등주 진입정책 검증" 요청 시:
  `E:\TMP\검증프롬프트 세부\급등주_진입정책_로직검증.txt`
- "분할매매 검증" 요청 시:
  `E:\TMP\검증프롬프트 세부\분할매매 로직검증.txt`  <!-- 파일 없음 (2026-08-18 확인) - 복구 대기 -->
- "손절기준 검증" 요청 시:
  `E:\TMP\검증프롬프트 세부\손절기준 로직검증.txt`
- "공통 운영감사 기준 검증" 또는 "공통 운영감사" 요청 시:
  `E:\TMP\검증프롬프트 세부\공통_운영감사_기준.txt`
- "데이터 원천 수집품질 검증" 요청 시:
  `E:\TMP\검증프롬프트 세부\데이터 원천 수집품질 검증.txt`  <!-- 파일 없음 (2026-08-18 확인) - 복구 대기 -->
- "SSOT 상태파일 계약 검증" 요청 시:
  `E:\TMP\검증프롬프트 세부\SSOT_상태파일_계약_검증.txt`
- "배치 스케줄 락 재시작 검증" 요청 시:
  `E:\TMP\검증프롬프트 세부\배치 스케줄 락 재시작 검증.txt`  <!-- 파일 없음 (2026-08-18 확인) - 복구 대기 -->
- "Config Lock 환경변수 검증" 요청 시:
  `E:\TMP\검증프롬프트 세부\Config_Lock_환경변수_검증.txt`
- "백테스트 검증데이터 품질 검증" 요청 시:
  `E:\TMP\검증프롬프트 세부\백테스트 검증데이터 품질 검증.txt`  <!-- 파일 없음 (2026-08-18 확인) - 복구 대기 -->
- "대시보드 표시값 SSOT 검증" 요청 시:
  `E:\TMP\검증프롬프트 세부\대시보드 표시값 SSOT 검증.txt`  <!-- 파일 없음 (2026-08-18 확인) - 복구 대기 -->
- "보안 백업 복구 검증" 요청 시:
  `E:\TMP\검증프롬프트 세부\보안 백업 복구 검증.txt`  <!-- 파일 없음 (2026-08-18 확인) - 복구 대기 -->
- "주식로직 방법론 내용검증" 또는 "방법론 내용검증" 요청 시:
  `E:\TMP\검증프롬프트 세부\주식로직 검증 방법론 내용검증.txt`
- "운영복구 재시작 중복실행 검증" 또는 "운영복구 검증" 요청 시:
  `E:\TMP\검증프롬프트 세부\운영복구 재시작 중복실행 검증.txt`
- "반복오류 재발방지 검증" 또는 "재발방지 검증" 요청 시:
  `E:\TMP\검증프롬프트 세부\반복오류 재발방지 검증.txt`  <!-- 파일 없음 (2026-08-18 확인) - 복구 대기 -->
- "뉴스 신뢰도 LLM 정성분석 검증" 또는 "뉴스 LLM 정성분석 검증" 요청 시:
  `E:\TMP\검증프롬프트 세부\뉴스_신뢰도_LLM_정성분석_검증.txt`
- "원장 복구 동기화 검증" 또는 "누락 체결 원장 동기화 검증" 요청 시:
  `E:\TMP\검증프롬프트 세부\원장 복구 동기화 검증.txt`
- "브로커 주문 디스패치 검증" 또는 "KIS 주문 디스패치 검증" 요청 시:
  `E:\TMP\검증프롬프트 세부\브로커 주문 디스패치 검증.txt`  <!-- 파일 없음 (2026-08-18 확인) - 복구 대기 -->
- "실시간 급등 LOB 검증" 또는 "급등 LOB 실시간 검증" 요청 시:
  `E:\TMP\검증프롬프트 세부\실시간 급등 LOB 검증.txt`
- "대시보드 상태빌더 안정성 검증" 또는 "상태빌더 안정성 검증" 요청 시:
  `E:\TMP\검증프롬프트 세부\대시보드 상태빌더 안정성 검증.txt`  <!-- 파일 없음 (2026-08-18 확인) - 복구 대기 -->
- "팔로우쓰루 검증샘플 적재 검증" 또는 "followthrough 샘플 적재 검증" 요청 시:
  `E:\TMP\검증프롬프트 세부\팔로우쓰루 검증샘플 적재 검증.txt`
- "매수로직 매수차단로직 검증" 또는 "매수차단로직 검증" 요청 시:
  `E:\TMP\검증프롬프트 세부\매수로직  매수차단로직 검증 프롬프트.txt`  <!-- 파일 없음 (2026-08-18 확인) - 복구 대기 -->
- "뉴스 implication 매매연결 검증" 또는 "뉴스 시사점 매매연결 검증" 요청 시:
  `E:\TMP\검증프롬프트 세부\뉴스_신뢰도__뉴스_implication_매매연결_검증.txt`
- "미래예측 로직 검증" 또는 "미래예측 검증" 요청 시:
  `E:\TMP\검증프롬프트 세부\미래예측 로직 검증.txt`  <!-- 파일 없음 (2026-08-18 확인) - 복구 대기 -->
- "배치파일 검증" 또는 "배치파일 프롬프트 검증" 요청 시:
  `E:\TMP\검증프롬프트 세부\배치파일 검증.txt`  <!-- 파일 없음 (2026-08-18 확인) - 복구 대기 -->
- "서브에이전트 인코딩문자열 상태 점검" 또는 "서브에이전트 문자열 점검" 요청 시:
  `E:\TMP\검증프롬프트 세부\서브에이전트 인코딩문자열 상태 점검.txt`
- "주식로직 방법론 기준 검증" 또는 "방법론 기준 검증" 요청 시:
  `E:\TMP\검증프롬프트 세부\주식로직 검증 방법론 기준.txt`
- "급등주 로직구현 검증" 또는 "급등주 E2E 검증" 요청 시:
  `E:\TMP\검증프롬프트 세부\급등주_로직구현_검증.txt`
- "급등감지 사후검증" 또는 "급등주 사후검증" 요청 시:
  `E:\TMP\검증프롬프트 세부\급등감지_사후검증.txt`
- "가상매매 급등매매 연결상태 검증" 또는 "급등 연결상태 검증" 요청 시:
  `E:\TMP\검증프롬프트 세부\가상매매_급등매매_연결상태를_검증.txt`
## 로직 운용 초기 기준
- 로직 운용 초기 상태는 별도 승인 없이 매수/매도, 주문, 체결, 원장, 통계, Gate, risk-lock, 점수, 임계값을 완화하거나 우회하는 근거로 사용하지 않는다.
- 운용 초기에도 `FAIL-CLOSED`, `BLOCK`, `HARD`, `risk_off`, `kill_switch`, `production_risk`, `risk_orchestration`, `P1 entry gate` 판정은 기존 정책대로 적용한다.
- 운용 초기 예외를 적용하려면 사용자가 명시적으로 범위, 기간, 대상 로직, 허용 동작을 승인해야 하며, 승인 전에는 read-only 검증과 원인 보고만 수행한다.
- 운용 초기 검증은 가상매매/읽기 전용 산출물로 분리하고, 실거래 연결 또는 차단 완화와 혼용하지 않는다.
- 운용 초기 관련 변경은 정책 변경으로 분류하고, 버그 수정과 분리해 백업, PLANS 기록, 3단계 검증을 수행한다.


## 17. 판단 품질 원칙
- 사용자 의견에 기계적으로 동의하지 않는다.
- 동의, 반대, 부분동의를 명확히 구분하고, 근거가 있으면 짧게 제시한다.
- 정책 유지도 하나의 판단으로 보고, 유지 근거와 부작용을 함께 보고한다.
- 검증 결과가 정책 변경 근거 부족이어도 원점으로 돌리지 않고, 다음 관찰, 표본, 후보 분류 산출물을 남긴다.
- 사용자가 반복 문제를 지적한 경우, 사과보다 재발 방지 행동 기준을 먼저 제시한다.

## 18. 매매 로직 판단 원칙
- 매매 로직 검증 시 제외 조건 검증과 매수 후보 탐색을 분리한다.
- 특히 급등주는 차단 사유 설명만으로 종료하지 않고, 매수 가능 후보, 탐색 후보, 진짜 제외 후보를 구분한다.
- 급등주 검증은 안전한지만 보지 않고, 손실을 감수할 만한 기대값 구조가 있는지 함께 본다.
- 차단값을 신뢰하기 전에 해당 값이 급등주 목적에 맞는지 먼저 검증한다.
- 차단 사유가 남아 있어도 관찰 가치가 있는 경우, 실매매 완화와 분리된 read-only 후보 레이어로 남긴다.

## 19. 진행 방식 보강 원칙
- 복잡한 로직 논의는 설명만으로 끝내지 않고, 가능하면 read-only 산출물 또는 체크리스트로 고정한다.
- 장중에만 검증 가능한 항목과 장전, 장후에 가능한 항목을 분리해서 보고한다.
- 장중 검증 필요 항목은 대기라고만 하지 말고, 확인할 입력, 산출물, 판정 기준을 미리 명시한다.
- 같은 유형의 답변을 반복하지 않기 위해, 반복 지적을 받은 경우 이번 진행이 이전 방식과 무엇이 다른지 명시한다.

## 20. 작업창 공통 진행 보고 기준
- 모든 작업창에서 사용자의 지시사항을 원문 기준으로 먼저 고정하고, 그 범위 밖으로 임의 확장하지 않는다.
- 보고는 항상 `지시사항 / 진행된 것 / 남은 것 / 다음 진행 / 다음 보고 기준` 순서로 짧게 작성한다.
- `진행된 것`은 실제 파일, 산출물, 실행 결과로 확인된 항목만 적고, 추정이나 의도는 포함하지 않는다.
- 산출물 생성, 대시보드 표시, 감사 파일 생성, 루프 실행은 전체로직 적용으로 판정하지 않는다.
- `전체로직 적용`은 전체 후보 판단, 최종 매수/대기/차단 판단, 또는 주문 전 판단 흐름에 실제로 반영될 때만 사용한다.
- 전체로직 적용이 아니면 `전체로직 미적용`으로 명시하고, `있음/없음`, `부분 적용`, `완료` 같은 표현으로 흐리지 않는다.
- 남은 작업은 왜 남았는지와 다음에 확인할 정확한 파일, 함수, 산출물 위치를 함께 적는다.
- 다음 작업이 끝나면 무엇을 보고할지 미리 한 줄로 명시한다.

## 21. 완료 보고 및 데이터 흐름 검증 규칙
- 코드 반영의 교차 검증 의무화: 파일 수정 후 즉시 수정한 코드가 파일에 정확히 쓰여졌는지 원문(grep/view)으로 재확인한다. 치환 실패를 성공으로 추정하여 보고하지 않는다.
- End-to-End 데이터 흐름 런타임 증명: 코드를 작성한 것으로 완료를 선언하지 않는다. 새 워커를 만들었다면 스케줄러나 루프에 의해 실제로 기동되는지 증명해야 하며, 시스템 간 연동 시 데이터가 실제로 도달하여 변수로 평가되는지 런타임 로그로 확인하기 전까지 연동 완료라고 보고하지 않는다.
- 팩트 기반 보고: 추정이나 의도를 섞어서 보고하지 않으며, 오직 확인된 산출물 경로, 확인된 로그값, 확인된 코드 원문만을 바탕으로 사실만 건조하게 보고한다.

## 22. 정직한 E2E 보고 템플릿 표준 (사용자 지침 반영)
작업 완료를 보고할 때는 추상적인 요약이 아닌, 다음 항목들을 명확히 구분하여 사실 기반으로 서술한다.

### ● 완료한 작업
- 코드의 어느 위치(함수, 라인 등)에 어떤 로직이 추가/변경되었는지 구체적으로 명시한다.
- 변경된 로직이 다른 시스템에 부작용을 일으키지 않도록 격리되었는지(예: 별개 서브시스템) 함께 설명한다.

### ● 직접 실행해서 검증한 것
- 실제로 기동(mock 모드, 스크립트 실행 등)하여 런타임에서 확인한 내용만 적는다.
- 확인된 로그, 구독 성공 여부, 파일 생성 등 구체적 성공 기준을 나열한다.

### ● 검증하지 못한 것 (정직하게 고백)
- 환경적 제약(장 마감, 외부 연동 불가 등)으로 인해 끝까지 검증하지 못한 한계점을 숨기지 않고 명확히 기재한다.
- 프로듀서/컨슈머의 규약 일치 등 구조적인 이론과, 실제 데이터 흐름이 확인되지 않은 한계를 구분한다.

### ● 사소한 흠이나 관찰 사항
- 메인 로직에는 영향이 없지만 발견된 코드상의 어색함(예: 패딩 버그 등)을 기록해둔다.

### ● 다음 스텝 / 확인 권장사항
- 미검증 항목을 확인하기 위해 특정 시간대(예: 익일 09:00 이후)에 사용자가 직접 확인해야 할 파일 갱신 여부나 로그 위치를 구체적으로 안내한다.


## 23. 전략 방법론 검증 가드
- 전략, 후보 생성, 국면, 신호, 실행 가설을 검증하기 전에는 `E:\1_Data\docs\references\STRATEGY_VALIDATION_GUARD.md`를 반드시 읽는다.
- **연구 목적의 모든 측정 라운드**(재측정·재현·기준선 교체·축 탐색·전략 대 대안 비교 포함)에는 `E:\1_Data\docs\references\NEW_SIGNAL_VALIDATION_STANDARD.md`를 함께 읽는다. [2026-09-11 추가] 이 문서는 2026-08-31 v1.1 로 적용 범위가 가장 넓은데 AGENTS.md 어디에서도 참조되지 않아 한 번도 열리지 않았다.
- 시작 보고에는 `가드 읽음`, `현재 고정 목표`, `방향 변경 여부`, `이번 검증 범위`를 명시한다.
- 가드의 금지 사항과 충돌하거나 범위를 바꿔야 하면, 실행 전에 근거와 영향 범위를 기록한다.

## 24. 브로커(KIS) 주문 API 제약 및 증거금 정책
- KIS는 시장가 주문 시 매수가능수량(psbl_qty_calc_unpr) 산출 단가를 상한가(전일종가 +30%)로 강제하며, 그 결과 매수가능수량(max_buy_qty / nrcvb_buy_qty)이 0으로 산출될 수 있다.
- 이는 소액 계좌 한정 현상이 아니며, 시장가 주문 시 항상 가용현금의 약 77%(1/1.3)까지만 사용 가능하도록 바인딩된다.
- 주문 구분(ORD_DVSN)이 01(시장가)일 경우, 인자로 넘긴 가격(ORD_UNPR)은 완전히 무시되고 상한가가 강제 적용된다.
- 따라서 포지션 사이징 시 잔액을 최대한 활용하거나 테스트를 할 때는, 시장가(market) 대신 지정가(limit)를 사용해야 주문 실패를 방지할 수 있다.
## 25. 지표 인용 규약
- 수치를 인용할 때 조건을 함께 명시한다. 조건이 빠진 수치는 인용하지 않는다.
- 지표별 명시 항목은 [`E:\1_Data\docs\references\METRIC_CITATION_PROTOCOL.md`](E:\1_Data\docs\references\METRIC_CITATION_PROTOCOL.md)에 정의한다.
- 특히 다음은 조건에 따라 몇 배씩 달라지므로 조건 없이 쓰지 않는다.
  - 후보 수 / 후보 발생일: 측정 창, 래더 적용 여부, 섹터 유니온 포함 여부 (래더만으로 6.5~12배)
  - 진입 통과율 / `entry_ready`: 집계 단위, 카운터 리셋 주기, `max_new` 값
  - `final_score`: 어느 파일인지(base / 사이드카 / `entry_decision_rows`), 축 구성, 측정 시각
  - 체결·손익: 원장 출처, 경로 분류 기준, 창 정렬
  - 인증 수치: 저장값 재채점인지 재계산인지
- 비율을 만들 때 분자와 분모의 집계 단위가 같은지 먼저 확인한다. 단위가 다른 비율은 만들지 않는다.
- 측정 착수 전에 `.agent\PLANS.md`와 메모리에서 **이전에 같은 질문에 도달한 기록이 있는지 먼저 검색한다.**
- 조건 누락으로 사고가 나면 그 사례와 함께 규약 문서에 항목을 추가한다.
